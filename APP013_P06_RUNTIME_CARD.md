# P06 runtime wiring card

Entry: backend d07ff35, governance 3c37077, fetched clean focused worktrees; owner authorized proceeding with runtime wiring. Existing APP-013/2B/P06 item 2, not a new package. Frozen and full consistency checks passed before edits. Acceptance remains same protected phone → confirmed eligible address → one job; no payment/booking/send authority. No scope deviation.

Finite item 2 implementation order:
1. Customer verify boundary: existing /customer-session/verify strict request schema, new optional server-owned controlledVerification port, mandatory current authorization before durable.execute. Reuse session/origin/TLS/body/budget gates and DurableVerificationService; never relax fixture verification/localPhone guards. START/CHECK only, US E.164, six-digit code, UUID IDs, explicit requested=true/current notice on both requests. Fixed safe receipt projects operationId/state/outcome and false downstream authority; no attempt/provider identifiers, phone, OTP, usage or raw errors. Missing port/policy/authority refuses. No automatic retry or fresh operation ID generation at this boundary.
2. Runtime loader and managed ingress: complete exact config/identity interface before implementation; reuse controlled composition and canonical authority, numeric keys, durable spend admission, exact candidate origin. No environment defaults that activate resources, blind forwarded-header trust or fixture-loopback flag in deployed configuration. Source mapping and implementation remain, not accepted by this card.
3. Same-page code entry and connected proof: replace synthetic pre-provisioning in the existing harness with browser START/CHECK through the real durable service and synthetic provider. Test wrong code/current proof/edit/expiry/replay/budget then same address/job path, desktop/mobile. Keep activation disabled. Still pending.

Step 1 exact files: controlled-customer-verification.ts/.spec.ts, customer-consent-browser-transport.ts/.spec.ts. Server-only constructor requires durable executor, immutable notice version and authorize(sessionToken) returning Promise<void>; future loader must supply current tenant/activation/policy/controlled-provider and durable-spend checks, not a no-op. Adapter does not manufacture controlled proof; only DurableVerificationService writes it. Receipt APPROVED is provider outcome, not current admission authority. Legacy ports unchanged. No schema, UI or cloud change in this step.

Step 1 tests: missing config, expanded fields, wrong action/phone/code/IDs/notice/requested; auth failure before execute; START/CHECK exact mapping; unsafe/mismatched/unknown receipt refusal; no payload leaks; non-fixture same-origin HTTP with explicit port versus unavailable legacy verify; session/origin/body guards unchanged. Existing durable service concurrency/idempotency/budget tests remain the implementation source of those guarantees. Full Jest/lint/build/architecture/audits, governance frozen/full consistency/21 regressions and whitespace required. No browser QA claim for step 1 alone; connected database/browser step 3 remains mandatory.

Rollback: omit controlledVerification port, leave main customerSessionHttp() unbound. Owner approves exact release/resources/caps later; implementer owns remaining wiring and evidence. Observable step 1 finish: real transport invokes only authorized durable executor and emits safe receipt, locally tested; not live phone verification or P06 completion. No provider calls, production writes, deployment, migration, IAM/secrets, charges or external action.
