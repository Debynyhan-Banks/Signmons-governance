# APP-013 / 2B — bounded correction-sequence repair

Owner approval: after reviewing Google's support reply, owner approved the correction fix and explicitly approved refusing corrections when process-local sequence state is lost or expired. No new package or acceptance denominator. Source: backend e3cd8fc, governance 6303ef8; fetched clean focused worktrees, backend PR 21. Initial frozen-baseline and complete consistency checks passed.

Gap: controlled verification discards responseId and OAuth transport rejects previousResponseId. Google validateAddress reference requires every correction follow-up to use the first responseId. Jayson (Google support, owner-supplied reply) describes short-lived session caching as intended and proprietary internal metadata as distinct from provider content; this is technical guidance, not legal certification.

Finite implementation checklist:
1. Strict optional UUID previousResponseId in existing transport; omit on first request.
2. Composition-owned bounded volatile cache, maximum five minutes and session expiry, tenant/session/policy/intent binding. Retain only first response ID after correction; consume once. Timer deletion, capacity refusal, no logs/browser/database/queue serialization. Existing durable operation history distinguishes follow-up from first request after restart. Missing/expired state refuses before provider dispatch; no cap refund, automatic retry or fresh-sequence fallback. Concurrent follow-ups cannot reuse state. No raw response cache.
3. Reuse current controlled service and same-page correction journey; preserve all address, phone, policy and transaction gates. Missing/invalid response ID prevents correction continuation. Completion/refusal consumes state; unrelated session cannot consume it. No county fallback expansion.
4. Tests: transport omission/validation, first-ID forwarding, expiry/loss/isolation/one-use, existing connected mobile/desktop correction and replay, full tests/lint/build/architecture/audits, disposable database/browser harness, governance consistency and regressions.

Expected seams: google-address.adapter.ts request type, google-address-oauth.transport.ts, address-operation-ledger.ts internal count, controlled-intake-verification.service.ts, controlled-intake-composition.ts, narrow correction cache and tests; existing synthetic browser harness. No schema change. Missing cache across instances intentionally stops this staging journey while preserving draft; seamless distributed recovery is excluded. Rollback remains absent/disabled controlled HTTP resources. No live calls, deployment, IAM/secrets, billing, migration or customer actions. Observable finish: existing correction flow sends initial ID on second synthetic request, while expired/lost state sends nothing; review-ready evidence, not live acceptance.

Sources: https://developers.google.com/maps/documentation/address-validation/reference/rest/v1/TopLevel/validateAddress ; https://cloud.google.com/maps-platform/terms/maps-service-terms . This approved narrow transient-ID exception supersedes the P01 sentence prohibiting any responseId cache, not its durable-record prohibition. The reply does not require expanding durable records or implementing county fallback.

Connected-test finding: intentId equals requestId and intentionally changes on explicit corrected submission. Cache key therefore binds stable tenant/session/conversation/category/policy/organization identity (superseding checklist item 2's proposed intent binding), while each operation keeps its own intent/revision checks. Initial connected regression refused safely with draft preserved; no acceptance claimed for that failed run. This is the same session-bound repair, not a new capability.

Closeout: all four finite items locally complete. Full 2,301 unit tests (3 skipped), build/lint/architecture, zero-vulnerability audits and full disposable database/browser harness passed, including eight connected cases at 390/1440 with first-ID wire assertions and no stored UUID. See backend evidence/APP-013/google-correction-sequence.md for commands, artifacts, limits and owner review. No live demonstration or repair owner acceptance claimed.
