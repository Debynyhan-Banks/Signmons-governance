# 2B implementation card — protected verification to one reviewed job

## Status and authority

Current P01 specification: APP013_P01_ENGINEERING_CONTRACT.md is owner-accepted and supersedes unresolved design alternatives below for service identity, priority, minimal job fields and transaction/replay. P02 accepted; P03 controlled verification is locally review-ready with evidence/APP-013/p03-controlled-verification.md and APP013_P03_SOURCE_CARD.md. No blanket Google reply is required for the reviewed minimal-business-record interpretation. Next P04 actual atomic job/current-draft/replay integration under that card; P05 customer wiring and P06 explicit resource/release/run approval remain. This card's earlier “not implementation-ready” notes are historical at the design level, not permission to infer live readiness. No live acceptance or frozen-criterion change.

Planning-only source inspection at backend 76d48e16e0154e3649364150f9e1e54f3f761690 and governance d1b1689. Both origins fetched; safeguards are now merged separately at backend 8f571c3 and governance 59b6b91. This card supplements, and does not modify, the frozen APP013_REMAINING_EXECUTION_CONTRACT.md. Product feature branches are preserved; this does not merge them or claim they already include main's newer controls.

Section: existing **2B**, internal checklist item “Resolve the implementation contract.” Owner-reviewed amendment APP013_2B_AUTOMATIC_ADMISSION_CHANGE.md is adopted: Signmons automatically creates exactly one job after explicit customer submission and trusted current verification/tenant-policy checks; operators handle exceptions only and cannot bypass proof. No payment, booking, dispatch or notification authority. Accepted walkthrough remains 3/8 (37.5%). Approved scope amendment only; no additional scope deviation. Earlier bootstrap inspections below are historical source findings, not a mandatory operator step.

**Design proposed, not yet implementation-ready:** source and patch map below are concrete; provider-derived retention authorization and the exact protected customer bootstrap/release configuration remain gates. Do not mark the first checklist item complete or begin implementation until those decisions are resolved. No new standalone screen or transport rehearsal is the next step.

## Verified gaps, not inferred missing features

### Terms-first job-record design adopted — 2026-09-13

Owner approved proceeding after the field-by-field published-terms review. Design order for this work is published provider rules → field provenance/purpose/lifetime → implementation; a preferred schema is not a reason to invent a retention permission. Source baseline backend b05d8dd, governance 933e452. This adopts data minimization, not legal clearance, live activation or a completed 2B implementation contract. No scope deviation.

Permanent job record: retain Signmons-generated job/tenant/customer/session/request IDs and timestamps; customer description, service category, customer-confirmed address and its confirmation event; our configured policy/version, operational priority and actual job-creation action. Retention of independently supplied/generated business data is a provenance-based design interpretation, subject to our privacy policy, not an express Google grant. Identity linkage and confirmation must not be inferred from an API success. Never store a provider response under these fields.

Address fields: formattedAddress is customer-supplied or explicitly customer-confirmed data. A Google suggestion remains separately attributed temporary content until confirmation. Do not copy the complete addressComponents object; only the existing listed temporary correction fields/purposes are in scope. Keep googlePlaceId null unless actually supplied by Google; the published Place ID exception permits storage, but does not grant current verification authority. Keep coordinates null in this flow; permanent coordinates are not required. No historical rewrite.

Controlled policySnapshot.intakeAdmission must not serialize the fixture currentProof object. Exclude county, Google verdict, responseId, coverage outcome and permanent addressVerification/realVerificationAccepted assertions. Exclude hashes/digests of those outputs. addressReference/coverageReference cannot lead to stored provider payloads; use an internal request/operation ID only for operational correlation, not reconstruction of a provider result. coveragePolicyVersion means our configured rule version, not a Google-certified address outcome. A replay digest may cover customer-confirmed submission and internal policy/identity fields, never raw or derived Google proof. Preserve fixture history as fixture evidence; do not relabel it real or rewrite existing receipts.

Temporary evaluation: Google response → current eligibility evaluation → permitted job transaction → discard unnecessary provider content. Do not persist the response for future operator review or restore it after a crash. Correction retains only the allowed candidate fields, not county/verdict; subsequent validation remains explicit and budgeted. The final job record reports job creation, not a reusable Google validity certificate. Proof checks at creation remain required; discarding evidence does not weaken the checks themselves.

Exact residual question: does retaining a job-created action with the configured policy version, without provider fields or verification assertions, fall within prohibited content derived from Maps content in this use? This is the narrow interpretation to settle, not a demand for blanket permanent-proof permission. The implementer must also finish the request-scoped consumption/replay mapping; neither point is silently closed by naming fields differently. A support/qualified-terms review, if needed, should concern this exact record, not the rejected full currentProof payload. Do not send a new inquiry without authorization.

Published basis reviewed: [Address Validation caching table B.1.3](https://cloud.google.com/maps-platform/terms/maps-service-terms), [general restrictions and content definition](https://cloud.google.com/maps-platform/terms), [Place ID and attribution policies](https://developers.google.com/maps/documentation/address-validation/policies), [Google correction workflow](https://developers.google.com/maps/architecture/high-volume-address-validation). These are public non-EEA terms, not evidence of a special negotiated account agreement. Temporary address fields have purpose/expiry restrictions; no blanket county/verdict caching exception was identified.

Required implementation checks inside existing 2B: reject unexpected serializer keys; sentinel Google county/verdict/body strings never appear in job snapshots, audit, errors or logs; customer confirmation remains attributable; internal references cannot retrieve provider bodies; changed/expired proof cannot create a job; same committed request replays without provider calls; customer edits cannot reuse prior proof. These are planned tests, not passing runtime evidence.

### Approved automatic-intake priority policy — 2026-09-13

Owner answered “yes” to the explicit policy question: routine eligible intake creates a STANDARD-priority job, existing life-safety warnings stop normal intake and show established emergency guidance, unclear cases go to human review, and no automatic dispatch follows. This closes the product priority decision, not the entire implementation card. Source baseline backend 81e0f8b, governance e7f762a. No scope deviation.

STANDARD is the operational default for an otherwise eligible job, not a technical diagnosis, proof of safety, response-time promise or statement that urgency was professionally assessed. The automated receipt must not say humanReviewed=true or OPERATOR_OVERRIDE. Proposed policy label AUTO_INTAKE_STANDARD_V1 distinguishes this default from an operator's later authorized urgency decision; it is not a new API or live configuration value.

Reuse LifeSafetyService's existing escalation response, without expanding its pattern list in this slice. Inspect all trusted customer-authored turns and the submitted description at the final revision, not just the latest summary; an earlier escalation must not disappear because a later draft omits it. Do not scan assistant replies as customer assertions. An existing escalation remains exception-only; editing text cannot silently clear it. A detector non-match is not proof of safety. Missing/invalid history, unresolved warnings, inconsistent input or unqualified policy must not default into an eligible job. Exact exception-state binding belongs in the existing source/transaction patch map; no substitute demo.

Required regression cases within 2B: ordinary eligible draft uses STANDARD and an unassessed/default label; life-safety warning creates no normal job and preserves established guidance; earlier warning survives later omission; malformed/unavailable history refuses; unresolved exception cannot be self-cleared; operator urgency routes retain role checks; automatic admission grants no booking/dispatch/send/payment authority. These are required tests, not newly passing evidence.

Remaining implementation entry work is the exact service activation binding and permissible final verification-to-admission record lifecycle. Do not request this priority decision again or reintroduce routine operator approval. This approval does not authorize provider retention, live verification, migration or deployment.

### Automatic service and transaction mapping — 2026-09-13

Source: backend b9d6045, governance 663d924; origins fetched and original-plus-amendment baseline passes. This is source-qualified design, not implemented behavior. The following replaces the old assumption that admitReview/persistAdmission can be connected directly to a customer submit. Existing operator exceptions remain guarded. No new acceptance section.

| Boundary | Inspected behavior | Required automatic-path design |
| --- | --- | --- |
| Browser submit | customer-consent-browser-transport.ts submit requires confirmed=true, validates draft/revision, calls submitReview, then explicitly accepts only PENDING_REVIEW/jobCreated=false | Add a distinct controlled submit port and strict admitted/correction/exception result validator; preserve the old review-only contract. Browser sends intent/session credentials, never service identity or policy authority. |
| Service identity | RequestContextData has user/tenant/role, no service principal; operator() requires owner/admin/dispatcher | Supply a private composition-owned authority to the automatic service, not a fake USER role or a request header. Bind activation, tenant and exact policy version; verify the customer capability independently and recheck activation/policy inside the write transaction. Proposed internal actor label: signmons-intake-admission-v1; not an IAM resource. |
| Tenant policy | organizationProfileV1 is company information/FAQs, not an automatic-admission approval; current-proof consumer checks approved organization and payment policy | Do not treat FAQ approval as automation permission. A narrowly scoped versioned activation policy must bind tenant, organization/payment versions, permitted service categories and automatic-admission enablement; absent/revoked/mismatched policy refuses. Exact loader stays part of the existing controlled activation design, not APP-017 policy-editor work. |
| Atomic write | admitReview binds digest to operator actor/review; persistAdmission writes humanReviewed=true, OPERATOR_OVERRIDE, NOT_VERIFIED flags and USER audit | Factor shared atomic persistence from caller-specific authorization. New receipt version must distinguish service decision and customer confirmation; no false human-review or verification labels. AuditActorType already has SYSTEM_AI, usable for the deterministic Signmons service with an explicit deterministic-policy decision label, not a claim an LLM verified anything. Keep legacy receipts/version interpretation unchanged. |
| Lock/replay | lockCustomerConsentSession locks session, active tenant and conversation; receiptOnly is documented operator-only; prior job lookup uses tenant/intakeSessionId | Do not reuse receiptOnly=true from a customer path without a separately authenticated receipt-read contract. New-write checks remain mandatory. Replay returns a committed same-session/same-intent result; it must not create a job or revive proof after expiry. Missing current customer authorization requires authenticated recovery, not broad receipt disclosure. |
| Location storage | PropertyAddress requires googlePlaceId/latitude/longitude; persistAdmission currently writes randomUUID(),0,0 with the customer address | Do not copy placeholders into the controlled path. The schema cannot currently represent absent provider location. See the explicit minimal change proposal below; no migration is performed by this mapping. |
| Urgency | review() requires OPERATOR_REVIEWED_INTAKE and caller-selected urgency; legacy JobsService accepts normalized intake urgency with operator-verification note | Do not invoke the legacy job path as an authorization shortcut or invent a successful safety assessment. The controlled policy must define the existing urgency field's meaning and exception behavior before coding; no automatic emergency dispatch or LLM-derived permission. |

Proposed evidence sequence: explicit submit authenticates session and reserves the permitted operation outside the job transaction; the trusted adapter processes the current address response in that same request; correction produces only an allowlisted candidate and no admission. A matching, already customer-confirmed address can be checked and consumed in the subsequent short transaction with current phone proof, database time, revisions and policy. Response-derived county/validity information must not be persisted or passed through a browser token as a substitute for authorized storage. Request-scoped evidence is not an across-request cache or a guarantee of retention permission. Correction/restart may need a separately explicit validation request within the existing two-request envelope; no silent extra charge or new allowance.

Final receipt design is not yet closed: the current consumer serializes county/reference/expiry into currentProof. The controlled version must not copy that object into policySnapshot. Retain customer-confirmed address, internal operation/request/job IDs, actor and approved policy version only after their exact meaning/use is qualified; an internal ID or boolean cannot launder provider-derived facts. The Google high-volume guide supports specified component correction flags, not an express blanket authorization for county/admission records. Source checked: https://developers.google.com/maps/architecture/high-volume-address-validation (2026-09-13). This narrows the unanswered lifecycle question without claiming a legal resolution.

#### Minimal schema proposal — approval required before implementation

Owner approved this exact narrow proposal with “yes procedd” after the 3de3d50/8b5c84e handoff. Implementation authorization is limited to nullable location fields, the two existing job writers and local migration/regression tests; no staging/production migration. This supersedes the pending-approval language below for this slice only. Source baseline 3de3d50. Finite plan before coding: change three Prisma fields to optional; add an ALTER COLUMN DROP NOT NULL migration with no data updates; replace random/zero placeholders with null in both existing writers; assert null writes in their existing specs; inspect all consumers and typecheck; exercise migration preservation and multiple null/non-null uniqueness on disposable local PostgreSQL if available. Run full Jest, lint, build, architecture and governance checks. No UI behavior changes: browser QA is not applicable to this storage-only slice, and integrated automatic-admission acceptance remains open. Rollback is code rollback only until separately approved migration; never reapply NOT NULL to null-bearing data without a reviewed remediation. This is the explicitly approved schema exception, not completion of the full 2B card.

Demonstrated gap: mandatory location fields force invented values when the controlled path deliberately retains no coordinates or Google place ID. Proposed old/new: PropertyAddress.googlePlaceId String → String?, latitude Float → Float?, longitude Float → Float?. Preserve existing real values, tenant relations and unique non-null place IDs. New controlled addresses use null for unavailable fields, never zero/random placeholders. Audit nullable consumers and test their refusal/unknown behavior before accepting the migration. Do not rewrite historical values or run a production migration.

Alternatives: keep fabricated coordinates/IDs (rejected: misleading); require Google location storage for every job (rejected as a solution to this design because availability, retention and expiry remain constraints); nullable location fields (recommended, narrow local schema/migration change). No new table, provider or subsystem. Impact: existing 2B persistence and affected type checks/tests; five-section order and 3/8 acceptance unchanged. Proposed schema change is not yet approved by the earlier general automation-direction approval. Smallest owner decision: authorize preparing this nullable-field change and its local migration/tests, not deployment or database execution against staging/production.

Next after that decision: complete the exact activation/urgency policy and permitted receipt contract, then implement the finite existing patch map. Do not call this first checklist item complete: these dependencies are explicit, not assumed solved. The source mapping is complete for this pass; no runtime code, live requests or substitute demo were made.

### Ingress and pre-parse design qualification — 2026-09-13

Source baseline backend 99ba2ac, governance 3115803. This resolves the platform TLS and framework ordering questions in the prior bootstrap inspection, not the entire implementation gate. No runtime change or controlled request was made.

Official Cloud Run contract states TLS terminates at Cloud Run and the container receives HTTP; its HTTPS invocation guide documents HTTP redirects to HTTPS. Read-only service metadata confirms current ingress `all`, container port 8080/http1, latest-ready revision signmons-calldesk-staging-00065-guw. Therefore socket.encrypted cannot be the Cloud Run external-TLS proof. Keep direct-TLS/local-fixture behavior distinct from a proposed explicitly configured Cloud Run ingress mode. Runtime environment variables or a client Forwarded/X-Forwarded-Proto header alone cannot authorize that mode. Activation must bind the reviewed managed deployment, exact candidate origin and tenant policy; no global trust-proxy=true or forwarded-host-derived origin. Header canonicalization/spoofing and local/direct access refusals require integration evidence before activation. The sources do not establish arbitrary forwarding-header overwrite guarantees, so none is assumed.

Proposed request integration within existing task 3: in main.ts register a narrow customer-session handler before app.enableCors and before app.init/listen, with its own request-context wrapper. Only the exact protected route namespace is intercepted; unrelated routes call next without consuming their stream. For intercepted requests, check method/path/header/activation first, enforce a bounded streaming body with the existing readCustomerBrowserBody before JSON parsing, invoke the existing transport with its trusted server context, and finish the response without entering the default parsers. The handler must catch errors and emit the existing sanitized no-store response itself: global Nest guards/filters/pipes do not automatically protect early Express handlers. Separate operator routes retain their Nest guards and service role checks. Preserve rawBody:true and default parsers for Stripe/Twilio and all unrelated APIs. Do not disable global parsing or replace signature input with reserialized JSON.

The lockfile uses Nest core 11.2.3; its tagged source registers default parsers in init before module routes, while app.use registers directly on the adapter before init. The saved checkout's installed 11.1.18 is not the locked runtime and was not used as acceptance evidence. The early-handler approach is a source-backed design, not a passing integration test. Streaming deadline/concurrency release, malformed/duplicate headers, content encoding, 16,384-byte boundary, oversized chunked bodies, aborts, no double parsing, unrelated JSON and exact webhook raw-byte preservation must be verified in the existing integration harness. Never treat a proxy peer address as authenticated customer identity; existing per-peer budgets are only one layer beside durable account/session ceilings.

Sources checked 2026-09-13:
- https://docs.cloud.google.com/run/docs/container-contract
- https://docs.cloud.google.com/run/docs/triggering/https-request
- https://docs.nestjs.com/faq/raw-body
- https://raw.githubusercontent.com/nestjs/nest/v11.2.3/packages/core/nest-application.ts

Remaining gates are now explicit: owner-reviewed managed-ingress/activation policy and exact tenant/operator/purpose-bound key references, Google-derived retained-proof permission, then approval of the completed implementation card. These are not new sections. No further standalone demo or platform replacement is proposed. The first checklist item remains open until those decisions close; 3/8 (37.5%) unchanged. No scope deviation.

### Bootstrap source qualification — 2026-09-13

Read-only inspection at backend cc4a486; governance parent d79fa2f. Governance main has adopted intelligence documentation via PR #30 / 4e34d4b; this does not adopt the application branch. Temporary prior worktrees lost their .git links; fresh detached worktrees from the exact remote heads preserve their remaining files and both dirty saved checkouts.

**Concrete design recommendation, not deployment approval:** serve the existing customer journey and its `/customer-session/*` adapter on one explicitly allowlisted HTTPS candidate origin in the existing staging service. The server supplies one approved tenant/integration binding after checking the controlled activation policy; no browser-selected tenant and no browser integration credential. Reuse CustomerConsentCredentials for a newly issued, 15-minute, session-scoped capability delivered only in the initiating response and held in browser memory, never URLs or persistent browser storage. That capability identifies a session, not a verified person or messaging consent. Reject missing/expired/revoked activation and session state before any operation or provider liability reservation.

Source constraints that the adapter must satisfy:

- `WebchatIntegrationGuard` authenticates server-to-server bearer credentials through WEBCHAT_INTEGRATIONS_JSON and sets integration context. `CustomerConsentBrowserTransport` explicitly rejects Authorization and Cookie headers. Do not put that guard directly in the customer browser path, strip a customer credential to pretend validation, or expose an integration key. The proposed in-process server binding belongs to the controlled adapter, not a new public identity-header mechanism. Keep the existing server-to-server webchat route unchanged.
- `main.ts` enables rawBody but globally parses requests and CORS first. rawBody retention alone does not establish the transport's 16 KiB pre-parse limit, strict canonical JSON checks or rejection of duplicate headers. The route-specific bounded raw-body integration must be mapped and tested before registration; do not install a second conflicting parser or broaden global CORS.
- The transport requires actual trusted HTTPS/peer facts and an exact Host/Origin match. A Cloud Run external HTTPS URL does not prove the container socket is TLS. Never set encrypted=true unconditionally or trust arbitrary forwarded headers. The exact authenticated/trusted ingress-to-adapter mapping is still unqualified; it needs documented deployment behavior and forged-forwarding/direct-access refusal tests before this bootstrap is implementation-ready.
- Operator review remains separate: RequestAuthGuard verifies revoked Firebase tokens and tenant/role claims; TenantGuard alone checks only tenant presence. The continuation service enforces owner/admin/dispatcher roles. Preserve both guard and service-level checks; integration/customer sessions must never admit jobs. CustomerIntakeReviewController remains unregistered until the controlled composition, providers and proof source are ready.
- CustomerConsentCredentials has no runtime key loader or shared-key fallback. Do not assume STAGING_PHONE_SESSION_KEY has compatible purpose/key material. Select dedicated purpose-bound configuration only in the later owner-approved resource packet; never collect key values in governance evidence.

Read-only Cloud Run observation: existing project signmons, region us-east5, service signmons-calldesk-staging, runtime signmons-calldesk-runtime@signmons.iam.gserviceaccount.com; latest-ready revision signmons-calldesk-staging-00065-guw retains phone-preflight tag. Normal traffic is still 100% signmons-calldesk-staging-app013bounds. The observed candidate origin is https://phone-preflight---signmons-calldesk-staging-p572d6wipq-ul.a.run.app. This is a reuse candidate, not approved new deployment or proof that it serves customer intake. No configuration/identity/secret changes or provider calls were made. Historical suspended tenant/operator cannot be treated as active; exact activation binding and key references remain a separate reviewed packet.

Finite remaining bootstrap qualification: (1) verify trusted Cloud Run ingress facts and pre-parse route integration, (2) record the exact server-owned tenant/integration and separate operator binding plus purpose-bound key references without values, (3) test missing/forged/foreign/expired contexts and same-origin rejection in the existing harness. This elaborates task 3 below, not a new section or subsystem. Google retention remains independently unresolved. The first 2B checklist item is still open; no implementation, live acceptance or new demo is authorized by this inspection.

| Inspected source | Existing behavior | Required delta for 2B |
| --- | --- | --- |
| current-proof-admission.ts | CurrentProofSource and AdmissionSourceEvidence require FIXTURE_ONLY; consumer returns fixtureOnly true and realVerificationAccepted false; checks database time, tenant/session/revisions, approved organization/payment and county | Add an explicitly distinct controlled source/receipt contract; never rename fixture evidence to real or flip a return flag alone. Reuse the invariant checks. |
| customer-intake-continuation.service.ts | Optional admissionProof consumed inside admitReview transaction; exact replay and atomic job/link/consent/audit exist | Reuse this transaction through a distinct service-authorized automatic path; retain operator guards for exceptions. Disabled/missing real evidence or automation policy refuses admission. Existing fixture path stays isolated. |
| durable-verification.service.ts / verification-freshness.ts | Encrypted conversation ledger supports optional proof; freshness policy currently fixture-only | Separate source provenance from shared time/revision arithmetic; trusted provider CHECK may create controlled phone evidence only in current approved session. |
| staging-phone.service.ts | Registered phone-only route constructs durable service without freshnessPolicy and returns jobAdmissionAuthorized false | Reuse provider/durable primitives in controlled intake composition; do not reuse a historical phone-only result or silently expand its old approval. |
| customer-consent-browser-transport.ts | Bounded headers/body/origin/session transport with correction/address/verification ports | Wire real ports behind a server-owned approved tenant/bootstrap binding, not browser-supplied tenant or VERIFIED fields. Keep credentials out of browser/operator payloads. |
| customer-intake-review.controller.ts / communications.module.ts | Operator controller has auth/tenant guards but is deliberately absent from application module; continuation also not registered there | Explicit disabled-by-default service-authorized automatic path plus separately guarded operator exception routes; registering the operator controller alone is insufficient. |
| address-operation-ledger.ts / address-operation-executor.ts / google-address-operation.ts | Session/revision operation and cost identity exist but execution/policy are fixture-only; Google operation discards semantic body | Reuse schema/locks/reservations while defining separate controlled provenance, trusted request loading and allowed semantic-result ownership. File-based one-shot holds are not a distributed ledger. |
| google-address.adapter.ts / google-service-area.ts | Shared transient parser and Google-only coverage checks exist; review output has no admission authority | Consume qualified result under exact session/address/policy binding; keep correction, UNKNOWN and out-of-area distinct. |
| scripts/run-google-semantic-inspection.mjs and transient screen | Closed inspection discards selected address; not a persistent protected journey | Reuse useful presentation logic only inside the existing journey. Do not call this CLI from the app or run another standalone test. |

## Proposed end-to-end sequence

1. Server starts the existing protected customer session from an approved server-side tenant/integration mapping; source identity is not a body field. Operator authenticates separately through existing RequestAuthGuard/TenantGuard. Exact deployment origin, bootstrap credential owner and tenant allowlist must be recorded before activation; none is invented here.
2. Customer draft remains in the existing encrypted continuation/session path. Browser submits intent, never authoritative proof. Normalize phone; request notice opt-in separately from messaging consent. Reserve approved liability, perform provider I/O outside job-admission transaction, then bind successful CHECK to the same phone revision/session/current policy.
3. Customer submits current address/unit. Claim a durable address operation keyed to session/intent/revision/policy and reserve allowance before I/O. Load request from trusted draft. Parse response transiently. Missing/conflicting county/unit evidence is UNKNOWN; no ZIP fallback or mandatory GIS lookup.
4. If correction is needed, show original and candidate in that journey. Confirm selects the exact server-owned candidate; edit changes customer-stated draft. Either affected change increments address revision and invalidates affected address/coverage proof. It does not refresh old validation or send another request automatically. Revalidation requires explicit action within the approved operation budget. Cancel retains original draft and grants no new proof.
5. Successful validation plus explicit confirmation and IN_AREA may create controlled current evidence only under the approved field-retention contract. Phone remains independently current. Session/tenant/notice/source/organization/coverage policy and respective revisions must match; unrelated edits do not force another code.
6. Customer explicitly submits the customer-reviewed draft. A distinct trusted Signmons service path resolves tenant automation policy and current evidence; it does not impersonate an operator or accept browser-supplied authority. Reuse existing session locks and job transaction to recheck proofs with database time/current policies and atomically write one job, permitted evidence reference, links/eligible consent and service-attributed audit. No provider calls inside this transaction. Exact replay returns the committed receipt; changed replay refuses. Corrections stay inline; unresolved exceptions use the separately guarded operator review path, which cannot bypass verification. No routine operator login or approval is required for successful intake.
7. Session close/expiry/revocation stops new authority immediately. Cleanup follows existing accepted policy and never releases unknown cost holds. Controlled proof never converts into payment, availability, booking or messaging permission.

## Google-only data adaptation — owner-approved direction, 2026-09-13

Owner requested keeping Google and adapting the app to its published guidelines, then approved proceeding. This replaces the prior preference for durable provider proof at all costs and the alternative-provider recommendation; it does not change frozen acceptance or authorize activation. No scope deviation. Public terms checked below are the implementation design basis, not a legal certification.

The existing customer draft is the recovery record. An explicit confirmation stores the selected address as customer-confirmed data with revision and confirmation time, separate from any Google cache. Confirmation does not turn county, validity flags or coordinates into customer-owned verification evidence. Never label a recovered address as currently verified merely because it remains in the draft.

For temporary candidates, allow only formattedAddress, postalAddress, addressComponent.componentName and USPS standardizedAddress for address reuse; allow the listed component confirmation/correction flags only for correction requests. Proposed application expiry is the earlier of session end or 15 minutes from receipt, without sliding renewal. Google's table permits these fields for specified purposes up to 30 days with deletion or customer-confirmation/correction replacement; that ceiling is not the app default. Do not retain coordinates for this section. Reject unknown fields before serialization; exclude caches from logs, audit, backups and diagnostics. Expiry reads refuse immediately even if physical cleanup is delayed; cleanup must delete the candidate on expiry, cancellation and session closure. Verify the actual storage/backup path before claiming deletion acceptance.

Raw response, USPS county/FIPS, verdict and responseId are not added to durable storage by this adaptation. Place ID has a documented caching exception, but is not required here and does not establish current coverage. The exact handling of transient county/validity evidence across explicit customer submission and transaction-time automatic admission, and the meaning/retention of the resulting admission receipt, remains unresolved. Do not smuggle those outputs into a hash, boolean or policy snapshot. This narrow issue is not a prohibition on all Google processing or a requirement to await support before every design task.

Recovery: retain the customer draft and existing cost-operation records; never restore missing or expired validation authority. Show an explicit revalidation action only when current session, policy and remaining approved allowance permit it. No automatic provider retry or release of uncertain liabilities. A successful job receipt remains an idempotency/business record, not reusable address proof; its final evidence fields still need the lifecycle decision above. No provider call inside a job transaction.

Existing correction UI must visibly distinguish Google suggestions and provide Google Maps attribution under current guidance. Check public terms/privacy links before activation. Use fictional addresses only in offline fixtures, not live US Address Validation requests. These are checks inside existing 2B, not new sections.

Acceptance additions within existing test matrix: allowlist rejects unexpected nested fields; fixed TTL survives restart without renewal; confirm persists only the selected customer address; cancel/expiry removes candidate; restored draft grants no authority; uncertain provider outcome remains held; explicit retry reserves a new permitted operation; no Google output leaks into logs, backups or admission snapshots. Local tests do not constitute live acceptance.

Sources: [service-specific terms, B.1.3](https://cloud.google.com/maps-platform/terms/maps-service-terms) and [Address Validation policies and attribution](https://developers.google.com/maps/documentation/address-validation/policies), checked 2026-09-13. Preserve the current county/physical-address criteria. Before controlled-source implementation, finish the county/validity-to-admission lifecycle against these criteria and the already proposed bootstrap binding; if a frozen criterion must change, present the exact amendment rather than silently weaken it.

## Proposed data contract and unresolved permission

| Data | Proposed owner/storage | Lifetime / gate |
| --- | --- | --- |
| Customer-stated phone, address/unit and draft revision | Existing encrypted session/conversation records; customer-confirmed edits remain separately attributable | Existing approved session cleanup and submitted-job record rules; not a new blanket retention period. |
| OTP, OAuth token, raw Google response | Process memory only; no logs/audit/browser diagnostics/database copy | Discard on completion/error; never restore or replay provider response from logs. |
| Suggested Google address/components | Allowlisted server-owned candidate bound to session/revision; sanitized attributed display, as specified above | Earlier of session end or 15 minutes from receipt; no sliding renewal. Explicit confirmation replaces address fields with separately attributable customer data, never validity/county proof. |
| Phone verification proof | Existing encrypted ledger with operation reference, source/account identity, checked/confirmed/expiry, revision and policy versions | Existing 30-minute proof ceiling, limited by 15-minute session or shorter source expiry; historical closed proof cannot be revived. |
| Address/coverage proof | Request-scoped evaluation only; no proposed durable Google county/verdict/coverage result or responseId | Not a recovery cache. Current transaction binding remains to be completed; missing evidence requires a fresh explicitly permitted validation, not restoration. |
| Admission receipt | Minimal job action/customer submission/internal identity-policy record described above; no controlled copy of fixture currentProof | Provider-result assertions are excluded. The exact residual derived-content interpretation above remains explicit; no blanket permission claimed. |
| Costs | Existing durable operation/account liabilities; prior local holds untouched | Unknown outcomes remain held; no automatic retry/refund/reset. |

Recommendation: use the Google-only adaptation above and existing encrypted draft/operation storage. Recovery must preserve the draft, not guarantee resurrection of provider proof. The county/validity-to-admission lifecycle still needs resolution without weakening current acceptance; no replacement provider or new database is proposed. The historical Google case may inform that narrow decision but is not proof that all in-memory processing is forbidden. No legal compliance claim is made by this plan.

## Finite patch map after gates close

These are internal 2B tasks, not new sections. Each task must end in the same integrated acceptance path. Names of new files below are proposed, not existing-code claims.

1. **Controlled proof contract/source:** change current-proof-admission.ts, verification-freshness.ts and durable-verification.service.ts plus their specs; add controlled-admission-source.ts/.spec.ts for trusted transaction-time reads of approved records. Exit: fixture evidence cannot cross into controlled authority; provenance/current policy/time/revision tested. No schema expansion assumed—if existing encrypted records cannot support the contract, raise a change request first.
2. **Controlled provider composition:** reuse staging-phone-policy/admission primitives and address ledger/executor/OAuth/shared parsers; add controlled-intake-verification.service.ts/.spec.ts to own bound operations, proof writes and recovery. Changes expected to address-operation-ledger.ts, address-operation-executor.ts and google-address-operation.ts/.spec.ts must preserve existing fixture mode and holds. Exit: cap/refusal/unknown crash state truthful and no network inside admission transaction. Do not simply remove FIXTURE_ONLY checks.
3. **Protected application wiring:** add controlled-customer-intake.controller.ts/.spec.ts as an adapter over customer-consent-browser-transport.ts; update communications.module.ts and guarded operator controller/continuation composition. Exact raw-body middleware/filter/bootstrap binding to be specified against approved runtime before implementation. Serve/reuse the current customer/operator journey, update existing journey JS/HTML and operator controls where required; no third form or new frontend framework. Exit: real auth boundaries, preserved edited draft, no local fixture flag granting authority, disabled route cannot act.
4. **Integrated proof and controlled acceptance:** extend scripts/verify-current-proof-admission.mjs and existing customer/operator journey verifier through the current parent harness, not a new parallel harness. After local gates, prepare the single connected live-test packet with private identities and fresh rates/resources; owner approval required before any external action. Exit: actual current phone + eligible confirmed address → exactly one reviewed job, with owner evidence/sign-off.

No new payment/calendar/SMS-confirmation work, UI polish, generic cleanup/reconciliation framework, provider replacement, infrastructure purchase, production migration or automatic activation. Expected size remains high; no reliable calendar estimate while entry decisions remain unresolved.

## Required tests / evidence

- Positive connected journey: actual selected address/unit survives review submission; current phone/address/coverage consumed; one job; receipt distinguishes fixture/local from controlled evidence. Local synthetic proof is labeled as such.
- Refusal matrix: missing source/policy/notice; wrong tenant/session/account/revision; expired/revoked proof; wrong/expired code; missing/conflicting county/unit; adjacent county; non-US/non-OH; UNKNOWN provider/cap outcome. Draft retained, no job.
- Edit/cancel: affected proof invalidated, no auto-request, no old proof resurrection when changing back, unrelated fields do not invalidate phone.
- Concurrency/recovery: two submitters/exact replay, changed replay, audit failure rollback, source result arriving after edit/stop, expiry at consumption, interrupted dispatch versus held cost, restart/cleanup and historical receipt access. No network in job transaction; no falsely restored proof.
- Security: customer bootstrap versus operator auth, body/origin bounds, cross-tenant reads, redacted errors/logs/screenshots, no tokens/raw provider body in durable records, missing feature configuration refused.
- Automatic admission: successful customer submission without operator login; distinct service actor audit; callback alone cannot create a job; missing/revoked automation policy refuses; exception operator cannot bypass proof; duplicate/concurrent/lost-response submissions retain one job; all downstream authorities remain false. These extend the existing 2B matrix, not its section count.
- Browser: existing customer and operator flows at mobile/desktop, loading/error/correction/expired/retry states. No standalone screen screenshot counted as integrated acceptance.
- Controlled run: privately bind exact participant/environment/service accounts/notices, current rate ceilings, phone/address/shared caps, window, stop and reconciliation owner; no historical allowance reused. Preserve current live defaults until separate activation approval.

Commands after approved implementation: `npm run build`; `npm run lint`; `npm run arch:check`; `npm test -- --runInBand`; affected script tests; `npm --prefix ui test`; `npm --prefix ui run lint`; `npm --prefix ui run build` if UI changes; backend/UI full and production dependency audits with findings disposition. Existing parent integration command: `ORGANIZATION_EVIDENCE_DIR=<fresh disposable evidence path> PLAYWRIGHT_MODULE=<installed module> node scripts/verify-organization-profile.mjs`. Verify its disposable Unix-socket DB preconditions first; never point it at staging/production data. Run both baseline guards and cross-repository consistency checks. No live CLI is a regression command.

## Decision / approval register and stop condition

APP013_2B_ACTIVATION_PACKET.md supplies the requested proposed exact service/origin/tenant/operator binding, purpose-bound secret names, finite run envelope and separate implementation/configuration/release/send approvals. It is not executable: retention, runtime authorization, immutable implemented release references and a fresh approved window remain open. This proposal does not mark any gate complete.

| Gate | Responsible role | Exact remaining evidence / decision |
| --- | --- | --- |
| Retained Google evidence | Implementer supplies exact lifecycle; owner reviews material design changes with qualified terms advice if needed | Candidate allowlist/short TTL is specified above. Resolve county/validity consumption at automatic admission and final admission-record meaning without assuming permission for unlisted fields or weakening frozen criteria. No assumption that prior CEGIS exception applies. |
| Protected runtime identity | Implementer proposes; owner authorizes actual resource/configuration changes | Exact approved customer bootstrap origin/tenant mapping and operator binding; reuse existing resources where suitable, no invented identifiers or secret collection in docs. |
| Implementation design | Owner | Review controlled-source/record proposal and finite patch map once retention/bootstrap contract is complete. Current plan does not claim all assumptions resolved. |
| Connected live execution | Owner | Separate exact resources, fresh phone/address/shared allowances, participant/session/window, notices and stop/reconciliation responsibilities; prior willingness does not renew spent packets. |

Next permitted action is resolving the first two gates and completing their exact field/bootstrap configuration contract. If unavailable, report the missing evidence and stop; do not propose another helper. No scope deviation. This planning pass changes no runtime or acceptance result.
