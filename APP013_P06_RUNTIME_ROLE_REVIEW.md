# R06 limited runtime-role preparation

Current correction: native psql password initialization failed on Neon, and owner reports Console reset refuses a passwordless role. Do not execute step3 below or retry Console reset. APP013_P06_INITIAL_PASSWORD_REVIEW.md supersedes that credential method with a review-only proposal for a bounded adaptation of the existing private-input helper; not yet implementation or execution authority. Committed role/grants remain in place, NOLOGIN at last verified readback.

Owner approved preparing exact permissions/private credential setup after R06 identified no limited application login. Source backend838468ca7bb0ce025f5c059cfa751deaf784197d / governance95fbea2ff2380ac849df779610878dd8e619c796. Preparation/local tests only, no live role/password/secret/permission changes. Existing R06/R07/R08; no U03 or new task.

## Fixed preparation card — before local test changes

Three exits: (1) source-mapped, explicit SQL privilege manifest for p06_intake_runtime, initially NOLOGIN with no elevated flags, memberships, ownership or schema creation; (2) existing disposable runtime/browser journey exercised with SET LOCAL ROLE and negative privilege assertions; (3) exact private credential/expiry/revocation review procedure. Reuse existing controlled-runtime/startup/loaded-browser harness, not another application or provider test. Expected backend files: review-only SQL under scripts/fixtures, local role verifier and existing two runtime/browser verifier injection seams, evidence; no src/schema/dependency changes. Source map: controlled-intake-runtime/composition; customer-consent response/capture/session-lock; intake continuation; durable verification; address-operation ledger; shared browser budget; email-consent evidence; verification cleanup.

Permission boundary: SELECT-only policy/category data needs UPDATE on a harmless timestamp column for PostgreSQL row locks, not permission to edit settings/status/pricing. Customer name update, conversation lifecycle, address-operation transitions remain writable. Audit/events/evidence/link records are append/read, not arbitrary audit/hold deletion. Only expired communication-content/request-alias cleanup gets DELETE. No payments, appointments, SMS/email sending, migration history or broad all-table/default grants. UUID keys need no sequences. This is table/column privilege isolation, NOT new row-level security or per-tenant database isolation; app tenant checks remain required. PUBLIC inherited database TEMP/CONNECT and enum/function access must be disclosed/inspected, never claim individual REVOKE removes PUBLIC rights.

Tests: exact selected permissions, policy/state/price/audit mutation refusal, payment/migration read refusal, schema/role creation and privileged SET ROLE refusal; actual startup/phone/address/admission/correction/cleanup via limited role and existing synthetic provider ports; prior positive/negative browser cases preserved. Commands npm run build; existing verify-organization-profile.mjs with ORGANIZATION_EVIDENCE_DIR under /private/tmp and installed PLAYWRIGHT_MODULE, which creates/removes its own calldesk_org_* Unix-socket database; npm run lint; npm test -- --runInBand; npm run arch:check; governance frozen/full consistency/21tests, both whitespace gates. Local role must be collision-checked, transactionally provisioned and removed after proof; do not touch other databases, roles or the existing local server process. No live credentials used.

Private setup proposal uses PostgreSQL psql's existing hidden-input password command, not a new custom password writer. Qualify exact behavior using official PG18 docs; R07 must approve privilege SQL, fixed child identity, absolute password expiry and secret destination before execution. Any need for new production helper/code is a separate disclosed decision. No reset of inherited owner, parent/shared secret update, automatic retry or browser-SQL password entry. Unknown outcome leaves runtime disabled and requires readback.

Observable finish: explicit grants plus actual limited-role local journey proof and reviewed setup/revocation procedure. No live authentication/Neon grantor capability asserted from local superuser proof. R07/R08 still gate exact external execution. Size bounded to configuration qualification; no day estimate. Original12+U01+U02=14; seven closed/seven open, packages5/60,walkthrough3/8. No scope deviation.

## Qualified grant manifest

Backend scripts/fixtures/p06-runtime-role-review.sql (SHA256 a58335957d9892fd3055d48de30c6e89171e349525139976151ed0395cbfd5da) is the exact proposed initial NOLOGIN role/grant SQL, not a live installer. It has no password, target connection, LOGIN, CONNECT or expiry action. Apply only after fixed-child/collision/ownership checks inside an authorized transaction; do not run it blindly against a saved default connection.

| Object | Permission / inspected purpose |
| --- | --- |
| public schema | USAGE only, no CREATE or ownership |
| TenantOrganization, ServiceCategory | SELECT plus UPDATE(updatedAt) solely to permit FOR SHARE locks; no settings/status/price updates |
| Customer | SELECT/INSERT and UPDATE(fullName,updatedAt), existing intake customer upsert/session locks |
| Job | SELECT/INSERT plus UPDATE(updatedAt) for consent-binding row lock; no lifecycle/payment status update |
| PropertyAddress, ConversationJobLink | SELECT/INSERT, customer-stated address and exactly-once job link |
| CommunicationEvent, AuditLog | SELECT/INSERT, durable turns/review/audit/budget holds; no audit UPDATE/DELETE |
| AppointmentEmailConsentScope | SELECT/INSERT plus UPDATE(createdAt) for evidence serialization lock; no consent identity changes |
| AppointmentEmailConsentEvidence, AppointmentEmailConsentBinding | SELECT/INSERT only, optional consent evidence and job binding |
| Conversation, AddressVerificationOperation | SELECT/INSERT/UPDATE for encrypted state, lifecycle and durable provider operation; no DELETE of operation holds |
| CommunicationContent, AddressVerificationRequest | SELECT/INSERT/DELETE for content and expired request-alias cleanup; application predicates remain essential |

No sequences exist in the inspected migrated schema, so none granted. No public SECURITY DEFINER functions found in local migrated schema; recheck live before grants. No memberships, all-table/default grants, payments/calendar/SMS-policy/send-table access. The role is NOT database-row/tenant-isolated: it can read rows in its permitted tables and mutate allowed columns. It does not restrict UPDATE/DELETE to application-approved predicates. This bounded staging configuration is not a production multi-tenant RLS design. PUBLIC database CONNECT/TEMP/type/function permissions may still apply; no broad PUBLIC revoke or impact on existing consumers is proposed. Timestamp-column grants also permit timestamp changes; do not describe them as literally read-only.

## Private execution sequence for later R07/R08 approval

1. Operator verifies clean approved source, exact SQL hash, child endpoint ep-jolly-flower-ayc6w9hv.c-5.us-east-2.aws.neon.tech, neondb/public, child br-sparkling-sun-ay6gr5e8, current26migration history and absent p06_intake_runtime. Parent/shared DATABASE_URL and migration roles untouched. Current owner neondb_owner is used **only** for the supervised administrator connection. Confirm current grantor ownership/CREATE ROLE capability, inherited PUBLIC access and no active P06 consumers. Denial or collision stops; do not escalate permissions or rename around it.
2. Use installed PG18 psql with explicit host/port/database/user, verified TLS, -X (no startup scripts), -W (hidden administrator password prompt), ON_ERROR_STOP and PSQL_HISTORY=/dev/null. No credentials in argv, environment, browser SQL/history, chat, files outside encrypted storage or captured screen. Verify current_database/current_user and endpoint before BEGIN. Apply the reviewed NOLOGIN/grant SQL plus `GRANT CONNECT ON DATABASE neondb TO p06_intake_runtime;`; inspect flags/ownership/object permissions before COMMIT. No password yet and no runtime activation.
3. Set password_encryption to scram-sha-256 in this supervised session; invoke native `\password p06_intake_runtime`. Owner privately supplies a new unique password from their password manager at the two hidden prompts. PostgreSQL documents that this command sends an encrypted verifier, avoiding cleartext command/server history. The verifier remains sensitive; do not capture logs, transcript or role-password catalogs. This uses the native client, not the earlier custom migration-role password writer and no provider-support email requirement. Do not reset neondb_owner. Role remains NOLOGIN until a separately approved login window.
4. Owner privately enters the limited-role child connection URL into **only** the new projects/signmons/secrets/signmons-staging-p06-child-database-url version using the provider's secret-entry field. Encode password characters correctly; direct child endpoint, role p06_intake_runtime, database neondb, schema public, strict server-certificate verification. No inherited owner password or parent endpoint. Assistant must not inspect the value field/screenshot; wait for entry to be closed before metadata-only numeric-version readback. An uncertain upload is reconciled, never automatically versioned again. No custom credential helper or plaintext URL is created by this preparation.
5. R07 separately approves a concrete maintenance login window (maximum60minutes, absolute UTC end recorded immediately before action). `ALTER ROLE p06_intake_runtime LOGIN VALID UNTIL '<approved absolute UTC end>';` is a review template, not a runnable approval with a blank. Use a second explicit TLS psql connection as limited role, hidden -W prompt, to verify current/session user, effective privileges and denial checks without private record reads. Numeric child-secret reference is then used by the disabled candidate. Failure: NOLOGIN and stop; no automatic password reset/retry. R10 separately authorizes fresh login validity for its exact run window if needed; it does not inherit a previous window.
6. Run closeout first revokes matching runtime/phone digests and closes/purges the controlled session through existing approved paths. Then administrator sets only p06_intake_runtime NOLOGIN and terminates its sessions in neondb, excluding the administrator PID; verify zero remaining sessions and closed intake. Password expiry/NOLOGIN alone does NOT terminate pooled connections. Preserve role, grants, audit, job and held liabilities; no DROP OWNED/DROP ROLE against live staging. Secret disable/destruction remains separately scoped; disabled versions are still billable. Unknown outcomes stop for exact readback, not another run.

This procedure deliberately requires private owner entry twice (native password setup and exact secret field) instead of another custom handoff subsystem. It is preparation, not evidence of successful Neon TLS login, password entry, secret upload or deployment. Existing paused/disabled state remains until R07/R08 external authority. [PostgreSQL18 psql](https://www.postgresql.org/docs/18/app-psql.html) documents hidden password setup; [GRANT](https://www.postgresql.org/docs/18/sql-grant.html) documents privilege and PUBLIC behavior. Do not infer that the provider keeps no verifier/access logs.

## Local qualification result and review boundary

Actual limited current_user=session_user=p06_intake_runtime, no privileged memberships/flags;13 forbidden-operation checks including administrator SET ROLE, role/schema creation, role-password catalog, policy/price/state edits, hold/audit deletion and payment/calendar reads. Existing loader/phone test and eight loaded HTTPS browser cases (accepted/outside/unknown/correction at390/1440) passed using this actual limited Prisma connection. Fixtures and operator approvals still use the separate local administrator; provider calls are synthetic. No production code changed.

Local test adaptation: optional strictly bounded ORGANIZATION_SOCKET_DIR selects a private PG18 Unix socket in the existing organization harness and worker; required because pre-existing /tmp server was notPG18. No existing server stopped. Test role LOGIN without password is permitted **only** on the private local trust socket; it is never the live recipe. Created role/grants/database removed and owned server stopped. No acceptance tests weakened. See backend p06-r06-runtime-role.md for full results/failures.

All three preparation exits complete; runtime-identity design decision resolved by owner approval and local proof. R06 qualification is review-ready; R07 owner approval of the complete external diff is next. It must bind actual source/hash, profile wording, role SQL, secret destination, execution/cleanup windows and allowances before any write. There is no newly added task: original12+U01+U02=14, eight locally closed includingR06, R07-R12remain6; packages5/60(8.3%),walkthrough3/8(37.5%),P06unaccepted,ETAunvalidated. No scope deviation.
