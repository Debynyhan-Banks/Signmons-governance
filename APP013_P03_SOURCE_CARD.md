# P03 source card — current verification connection

Checkpoint: backend a4ef398, implementation and local synthetic-provider/database gates passed; review-ready, owner acceptance pending. Backend evidence/APP-013/p03-controlled-verification.md provides commands/results/limits. All four finite P03 implementation/validation items below addressed; P04/P05 integration and P06 activation are not claimed complete. The controlled phone arithmetic resides in controlled-phone-proof.ts/.spec.ts to leave the fixture-only freshness validator untouched. This is an internal file separation, not a new policy/package/subsystem. Actual product loader/phone spend approval ports remain composition responsibilities; the connected database test explicitly substitutes phone spend admission and provider I/O.

Entry 2026-09-13: backend f1534fc, governance 60621f9, both fetched/clean. Owner reviewed P02 and authorized proceeding. Existing APP-013/2B; accepted P01 engineering contract supplies authority, retention and finite P03 exit. No scope deviation. P03 remains active until the full exit is evidenced, not merely a helper commit.

Inspected gap: DurableVerificationService only issues FIXTURE_ONLY freshness; StagingPhoneService does not issue admission freshness at all. AddressOperationLedger and executor require fixture policy/context; GoogleAddressOperation discards its response. CurrentProofSource is explicitly fixture-only. Preserve all these legacy contracts; never rename a fixture result as real evidence.

Finite implementation:
1. Add distinct controlled phone policy/proof creation and transaction-scoped reader to the durable ledger. Bind account/service, session, phone digest and immutable policy; preserve revocation, timestamps, no-proof old receipts and all fixture tests. Reuse 30-minute maximum arithmetic, bounded by customer session. Controlled composition must use the actual bound Twilio adapter (network-free factory in tests).
2. Reuse address durable reserve/claim/complete tables and account/tenant/session caps through a separate controlled entry. P02 service authority replaces no operator guard; it is required under session locks on each controlled stage. Distinct policy hash/provenance, maximum two requests, finite eight-second deadline and held unknown liability. No schema or provider activation.
3. Add the proposed controlled-intake-verification.service.ts with trusted current-input loader, phone read, Google transport/shared parser/county evaluation, temporary allowlisted correction and single-use same-request transaction check. No raw Google response or county result in durable fields, browser success receipt, hash, log or queue. No provider call in final transaction. State/policy/revision edits and expiry refuse; a committed receipt must be checked by the P04 caller before this service is invoked.
4. Local tests for these paths plus existing ledger regression/disposable database checks, full backend tests/lint/build/architecture, audits, governance baseline/consistency/21 regressions. No UI registration/change: browser customer flow remains P05, job atomicity/replay integration P04. A transaction-state mock alone is not database locking proof.

Exact seams: verification-freshness.ts (reuse arithmetic without relaxing fixture validator); durable-verification.service.ts/.spec.ts; twilio-verify.adapter.ts bound provenance; address-operation-ledger.ts and address-operation-executor.ts with tests; new controlled-intake-verification.service.ts/.spec.ts. Existing current-proof-admission.ts remains fixture-only; new consumer does not persist its version1 fixture receipt. Existing scripts/verify-address-operation-ledger.mjs is the database regression harness. P01 commands apply; synthetic provider ports only. No CLI/live one-shot runner execution.

P04 supplies actual atomic job writer/current-draft loader under existing locks and edit invalidation integration; P05 supplies customer adapter/response mapping. P06 owns actual resources, immutable activation values, releases and paid run. P03 supplies the controlled callable connection and proves refusal/current-binding locally, not a live job. Implementation target remains one provisional package; report any unfinished checklist against P03 rather than adding a section. Rollback: leave controlled composition unregistered/absent. No charges, real data, migration, merge, deployment or external authority.
