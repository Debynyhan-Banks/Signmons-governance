# R02 existing backup tooling safety card

Result: all three finite local deliverables completed; see backend p06-r02-safety-result.md.13 guard tests,4 private-input tests, full PG18 managed26-table snapshot/negative/migration rehearsal and applicable repository gates passed. Actual credential/backup authority remains separate. No scope deviation.

Owner said proceed after legacy metadata correction and proposed local safety-code next step. Entry backend f35a66d, governance61b34fa; focused remotes fetched and clean; saved backend dirty work untouched. Frozen and full consistency gates passed before coding.

Trace: P06-R02 requires isolated migration access and usable recovery evidence before R03/R04; this enables safe recovery qualification for the same APP-013/2B automatic reviewed-job journey, not another product feature or acceptance section. Existing scripts/verify-p06-migration-upgrade.mjs uses separate120s child timers and whole-table assertions; inspection confirms no aggregate cap or privacy-safe streaming comparator. Reuse its full26-table shared-snapshot dump/restore, local managed-role fixtures, PostgreSQL18.6 and cleanup.

Finite checklist before coding:

1. Add scripts/p06_backup_guards.mjs: one monotonic budget (maximum20minutes, never renewed per command), monitored private workspace total768MiB, archive stdout writer capped64MiB before writing excess bytes, sanitized subprocess failures/discarded stderr, abort/child cleanup and no overwrite/symlink acceptance. Workspace monitor is stop-on-detection, not filesystem quota; report bounded polling limitations honestly.
2. Replace archive command/row assertion seams in existing synthetic rehearsal with these helpers; compare all26 tables through bounded FETCH cursors using deterministic JSON text/order in the same exported source snapshot and a read-only restored transaction. No retained customer row digests or diagnostic payloads. Sanitized metadata mismatches, row-size refusal rather than unbounded fetch. No external URL support/real CLI introduced.
3. Add scripts/p06-backup-guards.test.mjs: success, equal/mismatched/duplicate/empty rows, cursor chunking and failures; archive exact boundary/overflow/existing/symlink, workspace growth/insecure paths, shared deadline across operations, cancellation/failed subprocess stderr sentinel redaction; actual existing PG18 managed backup/migration rehearsal and private-input regressions.

Boundaries: only newly generated local fictional databases and files; source existing snapshot must remain alive through dump and source comparison. Local artifacts in private tmp, no claim of encryption; future real artifact/passfile only within separately verified encrypted ownership-enabled mount. No real credentials, cloud/provider connections, role creation outside disposable PG, customer rows, export, migration/deploy/billing, retention change or new dependency. No UI change; browser QA not applicable, existing customer walkthrough not accepted by tool tests.

Checks: node --test scripts/p06-backup-guards.test.mjs; python3 -B scripts/test_p06_private_input.py; P06_BACKUP_REHEARSAL=1 P06_ROLE_REHEARSAL=1 P06_MANAGED_REHEARSAL=1 with newly created private PG18 socket run existing migration script; node --check and Prettier check touched JS; npm run lint/build/arch:check; both repo baseline/consistency/whitespace and21 governance tests.

Finish: tested safety helpers integrated in existing full synthetic backup path with exact results/failures and cleanup evidence. Runtime remains disabled/unchanged; rollback is revert tooling commit, not any live database operation. Actual mount/handoff, exact approved UTC window and real backup proof remain separate existing R02 requirements, not new tasks. Owner approves external execution only after filled packet; implementer qualifies safeguards here. Medium local tooling scope; no promise of R02 completion date. R01 closed,R02-R12 open11,added0,accepted5/60 and3/8 unchanged. No scope deviation.
