# P06-R01 executable local rehearsal card

Owner approved v1 with "ok great lets proceed if any questions stop to ask proceed". Entry backend0744d3c/governance4bde29b; runtime/migration source53037fb unchanged. This card instantiates R01 only, not R02 or real migration authority.

Reuse the existing Unix-socket-only disposable PostgreSQL ownership guard from scripts/verify-organization-profile.mjs, exact26 migration files, installed Prisma7.10.0 and existing full DB/browser regression harness. Add scripts/verify-p06-migration-upgrade.mjs plus evidence; no application, schema or dependency edits.

Finite checklist: (1) export immutable migration inputs to an owned temporary directory, verify installed version and local socket identity; (2) deploy first13 with actual Prisma, seed fictional old consent/property data; (3) deploy pending13, verify preservation/default/increment/nullability, history/checksums and no-op second deploy; (4) compare catalog with fresh26-migration database and run existing constraint/behavior DB regressions; (5) on a separate owned old-schema DB, hold a conflicting lock, demonstrate migration connection timeout and failed-history stop with bounded child deadline; clean up only owned databases, without resolving history or killing other sessions. Capture actual failure boundaries, not atomicity assumptions.

Connection is constructed internally from /tmp socket, OS username, port5432 and random validated calldesk_p06_* database; never accept DATABASE_URL or real credentials. Prisma child uses isolated config and allowlisted environment. Fictional UUIDs/data only; no provider or browser calls from the new script. Any generated manifest/report is synthetic evidence. Explicit finally closes clients before dropping only databases successfully created by this run; failure cleanup must be reported, not hidden.

Validation: node --check scripts/verify-p06-migration-upgrade.mjs; node scripts/verify-p06-migration-upgrade.mjs; npm run build; npm run lint; npm test -- --runInBand; existing guarded verify-organization-profile.mjs with /private/tmp evidence and installed Playwright; architecture, Prisma validation, frozen/full governance/21 tests and whitespace. No new UI behavior: existing browser gate is regression only. Runner observes finite subprocess deadlines; timeout implementation must actually affect Prisma's migration sessions, not an unrelated connection. If that cannot be proven, stop R01 as incomplete and disclose the gap.

Finish: reproducible old-state upgrade/no-op/failure report and cleanup plus relevant existing behavior gates. R01 completion changes internal checklist only, not P06/2B acceptance. Recovery/provider target/old deployed revision compatibility remain R02. No scope deviation; no cloud, backup, secret, live data, migration or deployment permission.
