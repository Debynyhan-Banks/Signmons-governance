# FE-007 - Contact Capture (Email-Minimum) With Backend Persistence

## Ticket Summary

- Ticket ID: `FE-007`
- Repo: `Signmons-Marketing-Web-Feat-Marketing-Site` (+ backend support)
- Screen IDs: `SCR-PUB-009`
- Objective: make contact conversion real by requiring email-minimum and persisting submissions to backend storage.

## In Scope

- Frontend contact form validation: email required.
- Backend submission wiring to persisted storage endpoint.
- Deterministic success/failure UX states based on API response.

## Backend Contract

- Preferred endpoint: `POST /api/marketing/lead-capture`.
- If endpoint differs, update `SCREEN_ROUTE_API_MATRIX.md`, `LINK_CTA_MAP.md`, and `DATA_CONTRACTS.md` in same execution sequence.

## Acceptance Criteria

- [ ] Contact form rejects missing/invalid email.
- [ ] Successful submit returns persisted identifier (`leadId` or equivalent) and is shown/logged.
- [ ] Failed submit surfaces actionable error state.
- [ ] No frontend-only fake success path remains.
- [ ] Frontend/backend gates pass with evidence.
