# APP-012 - Payment Gate and Webhook Status Workflow

## Ticket Summary

- Ticket ID: `APP-012`
- Repo: `signmons-calldesk-backend` (+ frontend app surface)
- Screen IDs: `SCR-APP-006A`, `SCR-APP-006B`, `SCR-APP-006C`, `SCR-APP-023`, `SCR-CUST-002`
- Objective: operationalize payment-first booking with deterministic gate enforcement and webhook-status transparency.

## In Scope

- Payment link/deposit request management.
- Basic fail-closed Stripe payment-before-booking enforcement for Starter and every higher paid plan when the tenant's governed policy requires a booking fee or deposit.
- Advanced Growth-and-higher controls for configurable deposits, service-fee preauthorization, exception approvals, partial-payment policy, and payment recovery.
- Payment status and webhook event tracking:
  - paid
  - failed
  - expired
  - refunded/canceled
- Booking policy gate status:
  - booking unlocked/locked
  - required fee rules
- Customer payment/deposit status page.

## Out of Scope

- Storing card PAN data in Signmons systems.
- Processor-internal dispute lifecycle UI.
- Full accounting export workflows.

## Acceptance Criteria (must all pass)

- [x] Payment links and deposit requests can be created and tracked.
- [x] Webhook status is visible and fail-closed on validation errors.
- [x] Booking gate enforces tenant policy before dispatch readiness.
- [x] Starter can enforce a required booking payment; the implementation does not reserve the basic gate for Growth.
- [x] Growth-and-higher advanced payment controls are separate entitlements and do not weaken Starter's basic fail-closed gate.
- [x] Customer payment status is viewable via secure link.
- [x] Payment and gate actions are audit logged.

## Test and Evidence Plan

- Backend gates: `npm run -s build`, `npm test -- --runInBand`, `npm run -s arch:check`
- Frontend gates (if touched): `npm run -s build`, `npm run -s lint`, `npm test -- --runInBand`
- Evidence:
  - payment gate screenshots
  - webhook validation proof
  - booking lock/unlock proof

## Risks

- Webhook signature misconfiguration causing false states.
- Policy drift between UI and server enforcement.
- Customer confusion on partial/failed payment states.

## Bounded Checkpoint (2026-09-03)

- Backend branch: `codex/app-012-payment-gate`.
- Implemented the first vertical slice only: a shared fail-closed gate derives required payment from `depositRequired` or `serviceFeeRequired` and unlocks only on canonical `SUCCEEDED` state.
- Locked jobs remain outside dispatch readiness, return no eligible recommendation and reject new assignment without mutation or an audit side effect.
- `/app/dispatch` visibly explains the gate and disables assignment at desktop and phone widths.
- Payment/provider identifiers are not returned by the dispatch projection.
- Payment-request creation, signed/idempotent webhook processing, customer recovery/status and payment-transition auditing remain open; APP-012 stays in `Now` and is not release-ready.
- Evidence: `signmons-calldesk-backend/evidence/APP-012/readiness-report.md`.
- Estimate: APP-012 is approximately 20% complete; APP-006 through APP-016 is approximately 56% complete.

## Bounded Checkpoint (2026-09-04)

- Continued backend branch `codex/app-012-payment-gate` with one server-side payment-request section.
- Backend implementation commit: `88d4b3ef001ceb04dc973ddd0263c105f9a6a701`.
- Added authenticated, tenant-scoped `POST /jobs/:jobId/payment-requests` and privacy-safe `GET /jobs/:jobId/payment-request` endpoints for owner, admin and dispatcher roles.
- Request amount and currency come only from the job's pricing/policy snapshots; stale jobs, missing pricing, closed jobs, cross-tenant jobs and unready contractor payment accounts fail closed before provider access.
- Stripe Checkout is created as a direct charge on the tenant contractor's connected account with `applicationFeeAmountCents` fixed at zero. Signmons does not add setup, usage, booking, emergency, revenue-share or per-invoice fees.
- A version-4 `Idempotency-Key` is required. Only its SHA-256 hash is stored, provider identifiers stay out of API/audit projections, and an exact replay does not duplicate the success audit.
- Payment-request success and provider failure are tenant-scoped audit actions. Signed webhook ingestion and customer status/recovery remain open, so APP-012 stays in `Now` and is not release-ready.
- Migration `20260904100000_add_payment_request_tracking` is authored and verified only in an isolated local schema; it has not been run in staging or production.
- Evidence: `signmons-calldesk-backend/evidence/APP-012/readiness-report.md`.
- Estimate: APP-012 is approximately 40% complete; APP-006 through APP-016 is approximately 58% complete.

## Review-Ready Lifecycle Checkpoint (2026-09-07)

- Backend branch `codex/app-012-payment-gate` now contains the signed/idempotent Connect webhook transition, secure signed-link recovery/status, operator request/event controls, governed Growth-and-higher exception flow and release-readiness checklist completed in bounded commits after the September 4 request checkpoint.
- The webhook verifies the exact raw body, enforces signature freshness, maps the top-level connected account to a tenant, suppresses duplicate event IDs, validates trusted amount/currency and preserves monotonic payment/refund transitions.
- A final mode-boundary hardening section requires handled events' boolean `livemode` to match explicit `STRIPE_WEBHOOK_LIVEMODE` configuration before database access. Production configuration requires an explicit mode and rejects recognizable live/test Stripe key prefixes that conflict with it.
- Backend mode-boundary implementation/evidence commit: `27d595da21406704b6bc66804ba03d2e90643b23`.
- Operator/customer projections and audit/event storage remain bounded: provider account, event, Checkout, PaymentIntent and charge identifiers, payloads, secrets and Checkout URLs are excluded except for the immediate authorized Checkout response.
- Full backend build/lint, 28 suites and 224 tests, architecture, Prisma validation and the critical audit gate pass. Unchanged UI build/lint and 4 suites/17 tests pass. Existing September 6 desktop/390px browser evidence remains applicable because the mode section changed no rendered UI.
- Linked local and sandbox evidence has conditional acceptance. No persistent endpoint, credential, IAM policy, database migration, payment, deployment, merge, release or customer state was changed by the mode section.
- APP-012 remains in `Now` and unreleased. Continuous deployed staging acceptance, owner sign-off, merge and release require explicit approval.
- Estimate: APP-012 approximately 95%; governed APP-006 through APP-016 sequence approximately 68%.

## Commit Convention

- Commit message: `feat(app): implement APP-012 payment gate workflow`
- One focused commit only.
