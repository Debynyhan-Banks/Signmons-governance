# APP-008 - Dispatch Board and Technician Assignment

## Ticket Summary

- Ticket ID: `APP-008`
- Repo: `signmons-calldesk-backend` (+ frontend app surface)
- Screen IDs: `SCR-APP-014`, `SCR-APP-017`
- Objective: provide dispatcher command-center assignment workflows with recommendation rationale and safe manual override.

## In Scope

- Dispatch board queues:
  - new request
  - ready to assign
  - assigned
  - escalated
- Job assignment detail:
  - recommended tech
  - recommendation reason
  - assignment history
  - manual override controls
- Assignment actions:
  - assign
  - reassign
  - escalate
  - cancel

## Out of Scope

- Full autonomous auto-dispatch mode.
- Route optimization engine.
- Technician mobile UI beyond handoff signal.

## Acceptance Criteria (must all pass)

- [x] Dispatcher can assign and reassign jobs from board/detail.
- [x] Recommendation reason is visible before assignment.
- [x] Manual override is supported for authorized users.
- [x] Assignment actions are audit logged.
- [x] Tenant and RBAC boundaries are enforced.

## Test and Evidence Plan

- Backend gates: `npm run -s build`, `npm test -- --runInBand`, `npm run -s arch:check`
- Frontend gates (if touched): `npm run -s build`, `npm run -s lint`, `npm test -- --runInBand`
- Evidence:
  - dispatch board and assignment detail screenshots
  - assignment/reassignment API proof
  - audit event proof

## Risks

- Race conditions on concurrent assignment updates.
- Poor recommendation explainability reduces trust.
- Escalation logic inconsistency by urgency level.

## Implementation Evidence (2026-08-31)

- Backend build passed; 139 tests passed with 3 intentionally skipped.
- Architecture check passed.
- APP-008 backend files pass focused ESLint. Repository-wide lint remains blocked by pre-existing lint failures in `src/config/env.validation.spec.ts` and `src/logging/call-log.service.spec.ts`; APP-008 does not modify either file.
- Frontend build, lint and 8 helper tests passed.
- Browser QA passed at 390 px, 820 px and 1440 px with no horizontal overflow or console errors.
- Browser interaction proof confirmed that a non-recommended/unavailable candidate keeps assignment disabled until the operator supplies a normalized reason.
- API unit proof covers privacy-safe tenant queries, deterministic recommendations, assignment audit metadata, override reason enforcement, stale-write conflict handling, assignment cancellation and cross-tenant not-found behavior.
- Contract source: `DATA_CONTRACTS.md` section “APP-008 Dispatch Board And Technician Assignment Contract.”
- Frontend route: `/app/dispatch`.

## Commit Convention

- Commit message: `feat(app): implement APP-008 dispatch assignment board`
- One focused commit only.
