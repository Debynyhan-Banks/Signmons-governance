# APP-010 - Routing Rules, Service Areas, and Availability

## Ticket Summary

- Ticket ID: `APP-010`
- Repo: `signmons-calldesk-backend` (+ frontend app surface)
- Screen IDs: `SCR-APP-015`, `SCR-APP-016`, `SCR-APP-024`, `SCR-TECH-004`
- Objective: implement tenant-configurable routing with service area, technician availability, and escalation policy controls.

## In Scope

- Technician schedule/availability and on-call controls.
- Routing rules for:
  - service category
  - urgency
  - service area
  - business hours / after-hours
  - escalation policy
- Service area and trade coverage management.
- Recommendation output with assignment reason trace.

## Out of Scope

- Free-form autonomous AI assignment without rule constraints.
- Multi-objective route optimization.
- Cross-tenant shared rule coupling.

## Acceptance Criteria (must all pass)

- [x] Tenant can create/update routing and escalation rules.
- [x] Service area and availability constraints are enforced.
- [x] Emergency rules support owner + on-call escalation.
- [x] Recommendation reason is visible in assignment workflow.
- [x] Rule evaluations are audit logged.

## Test and Evidence Plan

- Backend gates: `npm run -s build`, `npm test -- --runInBand`, `npm run -s arch:check`
- Frontend gates (if touched): `npm run -s build`, `npm run -s lint`, `npm test -- --runInBand`
- Evidence:
  - routing rule and service-area screenshots
  - recommendation reasoning proof
  - escalation routing proof

## Risks

- Rule precedence conflicts.
- Unsafe defaults causing misrouting.
- Incomplete availability normalization.

## Commit Convention

- Commit message: `feat(app): implement APP-010 routing and availability`
- One focused commit only.

## Implementation Status

- Status: `Done` after merged release and isolated staging tenant acceptance on 2026-09-02.
- Backend PR `#11` merged at `b809b9d`; Cloud Run revision `signmons-calldesk-staging-00023-47g`; Firebase route `https://signmons-calldesk.web.app/app/routing`.
- Evidence: `signmons-calldesk-backend/evidence/APP-010/readiness-report.md` and `signmons-calldesk-backend/evidence/APP-010/continuation-validation-2026-09-02.md`
- Runtime acceptance created and updated a strict ZIP/available/on-call rule, configured an isolated technician through the deployed API, returned a covered `routing-v1` evaluation, and produced an eligible `dispatch-v2` recommendation.
- Real-business tenant and technician provisioning remains an operational onboarding/cutover concern and is not part of APP-011.
