# APP-013 - Notification Center and Messaging Templates

## Ticket Summary

- Ticket ID: `APP-013`
- Repo: `signmons-calldesk-backend` (+ frontend app surface)
- Screen IDs: `SCR-APP-021`, `SCR-APP-022`, `SCR-TECH-005`
- Objective: centralize owner/dispatcher/technician/customer communication signals and template controls.

## In Scope

- Notification center for owner/dispatcher and technician events.
- Messaging template management for customer communications.
- Transactional SMS and email templates for customer, technician, dispatcher, and owner communications, selected according to tenant policy, consent, event type, and delivery availability.
- Event categories:
  - intake created
  - urgency escalation
  - assignment update
  - payment status update
  - schedule confirmation/update
- Transactional appointment email after the calendar booking is committed:
  - ask for and retain the customer email once without repeating captured questions
  - confirmed date and Eastern Time arrival window
  - appointment reference and concise service type
  - private, absolute customer-management link
  - add-to-calendar attachment or standards-compliant calendar action
  - call/text fallback and private-link warning
- One updated customer email after a successful reschedule and one cancellation email after a successful cancellation.
- Delivery failure must not roll back a confirmed appointment; it creates an operator-visible failure with a safe retry path.
- Customer management credentials must not be copied into analytics, normal application logs or message-preview telemetry.
- Delivery status visibility (sent/failed/retry).
- SMS consent, STOP/HELP, quiet-hour, duplicate-prevention, provider-status, and safe-retry behavior must use the BE-008 communications foundation.

## Out of Scope

- External marketing campaign automation suite.
- Multi-channel social messaging orchestration.
- Advanced A/B experimentation framework.

## Acceptance Criteria (must all pass)

- [ ] Notification center displays role-appropriate operational events.
- [ ] Templates can be managed per tenant policy.
- [ ] Transactional SMS and email are independently configurable by permitted event, recipient role, consent state, and tenant brand policy.
- [ ] Appointment confirmation, reschedule, cancellation, assignment, technician-status, payment-status, and dispatcher-alert SMS events are idempotent and retain provider delivery/failure status.
- [ ] Delivery status and failures are visible.
- [ ] A committed eligible appointment sends exactly one branded customer confirmation email with the confirmed window, reference, private management action and calendar action.
- [ ] Reschedule sends exactly one updated confirmation; cancellation sends exactly one cancellation notice without an active management action.
- [ ] Customer email is requested once, retained in the conversation/job record and never repeatedly requested after capture.
- [ ] Customer email-delivery failure does not undo the job or calendar event and is visible to operations for retry.
- [ ] Secure management credentials are absent from analytics, ordinary logs and operator message previews.
- [ ] Sensitive data is redacted per role boundaries.
- [ ] Notification events are auditable.

## Test and Evidence Plan

- Current UI checkpoint: `/app/notifications` (`SCR-APP-021`) shows history and initial-confirmation/reschedule/cancellation/technician enqueue intents, with guarded owner/admin exhausted-intent retry review. Post-Calendar cancellation/reschedule intent/audit finalization is fixture-tested; owner/live acceptance and calendar reconciliation/pre-finalization gaps remain open. Historical checkpoints below are superseded by the latest bounded section.

- Backend gates: `npm run -s build`, `npm test -- --runInBand`, `npm run -s arch:check`
- Frontend gates (if touched): `npm run -s build`, `npm run -s lint`, `npm test -- --runInBand`
- Evidence:
  - notification center screenshots
  - template configuration proof
  - failed-delivery handling proof

## Risks

- Template drift from policy/compliance constraints.
- Alert fatigue for dispatch roles.
- Overexposure of customer data in message previews.
- Forwarded appointment-management links acting as bearer credentials; copy must warn customers to keep the link private and destructive actions may require an additional verification policy.

## Bounded Queue State Checkpoint (2026-09-08)

- Backend PR #21 provides four versioned customer SMS templates, tenant-bound operator queue/history and queue-admission validation against stored job/technician state.
- State-check implementation commit: `a04f1d7129495ed687a5ac896c5f29dd022a036c`. UI regression lint, 17 tests and build also pass; no new rendered-browser acceptance is claimed.
- Deleted jobs return 404; contradictory lifecycle copy or incomplete schedule data returns 409 before queue access. Valid cancellation, reschedule and EN_ROUTE cases remain eligible for the existing BE-008 controls.
- Backend validation: 16 focused and 302 full tests passed, 3 existing policy skips; build/lint/architecture/Prisma/critical audit passed. No UI changed and no provider send occurred.
- Admission uses a snapshot; concurrent mutations, completed-reschedule proof, automatic triggers and stale queue checks remain open alongside email, templates/preferences, UI and acceptance. Acceptance boxes stay unchecked.
- Estimate: APP-013 roughly 20%; governed APP-006 through APP-016 roughly 70%.

## Bounded Appointment Lifecycle Checkpoint (2026-09-08)

- Backend PR #21 now queues confirmation, reschedule and cancellation SMS only after the matching canonical appointment/calendar operation commits. Queue failure is isolated from the appointment result.
- Each automatic event uses a deterministic template/state digest as its idempotency identity. Manual queueing records the same digest, and the delivery worker revalidates it against the tenant-scoped job immediately before provider access.
- Missing/deleted, contradictory or changed state is dead-lettered before send; unverifiable earlier transactional records fail closed. The digest covers the rendered recipient, brand/timezone and schedule or technician state without exposing raw values in operator history.
- Focused validation passed 3 suites / 44 tests; full backend build/lint, 36 suites / 308 tests with 3 existing skips, architecture, Prisma and critical audit passed. Unchanged UI lint, 17 tests and build also passed; no new rendered browser artifact is claimed.
- Outbound delivery remained disabled. No provider message, configuration, migration, merge, deployment or real customer/job mutation occurred. A narrow check-to-provider concurrency race remains explicit.
- Automatic assignment/technician/payment/dispatcher events, email, preferences, UI and acceptance remain open. Estimate: APP-013 roughly 35%; governed APP-006 through APP-016 roughly 72%, planning estimates only.

## Bounded Technician On-The-Way Checkpoint (2026-09-08)

- Implementation: backend `477048abfa22a15ec63642f83519c180354341ea`, `feat(app): queue APP-013 technician departure notifications`.
- Existing technician status endpoint queues customer on-the-way SMS only after a changed status and audit commit. Duplicate/no-op retries and rejected/rolled-back writes do not queue. Queue/logging failure does not undo field status.
- Identity includes technician status timestamp; stale departure or reassignment fails the existing pre-send check. Earlier on-the-way hashes fail closed. No API response shape, UI, schema or provider configuration changed.
- Validation: 323 backend tests (3 existing skips), build/lint/architecture/Prisma, critical audit, unchanged UI lint/17 tests/build. Fifteen new regression cases cover ordering, failure boundaries, identity and obsolete delivery. No provider send occurred.
- Durable enqueue recovery is not implemented: post-commit failure/crash can leave status without a message, and status replay is not auto-repair. Remaining triggers, email, preferences, UI and acceptance stay open; no acceptance box is checked.
- Planning estimate: APP-013 roughly 40%; APP-006 through APP-016 roughly 73%. Review backend PR #21 and `evidence/APP-013/readiness-report.md`.

## Bounded Read-Only Notification Center (2026-09-08)

- Implementation: backend `3330c7bfa011120fa26f10b6582ab471f2ad3728`, `feat(app): add APP-013 read-only notification center`.
- `/app/notifications` (`SCR-APP-021`) consumes only SMS history with latest-100/query limits, status/job filtering, explicit UTC, and sent-versus-delivered distinctions. Navigation is available from dispatch. No send/replay/template-edit control is included.
- Password-masked operator token stays in memory; token/job changes and session clear invalidate prior responses. Generic errors and explicit metadata rendering exclude raw sensitive fields.
- Passed 323 backend tests (3 existing skips), backend build/lint/architecture/Prisma; UI lint, 23 tests/build; synthetic Chrome desktop/390px browser QA for filters, loading/empty/403/500, late-response/session clearing, no token storage and private-field omission. Screenshots and exact commands are in backend APP-013 evidence.
- Live backend/operator acceptance, durable enqueue recovery, remaining triggers, template/preferences controls, technician notification UI and email remain open. Broad acceptance boxes remain unchecked. No provider send or release occurred.
- Planning estimate: APP-013 roughly 50%; APP-006 through APP-016 roughly 74%.

## Bounded Technician Enqueue Durability (2026-09-08)

- Implementation: backend `fab7fa6a3c590cab3adbd8293616f0cb8fccd23f`, `feat(app): persist APP-013 technician notification intents`.
- Durable `SmsEnqueueIntent` capture is atomic with the technician EN_ROUTE status/audit transaction. Intent persistence failure rolls back the transaction; later queue/provider failure does not undo committed status. Disabled processing preserves pending intent without queue/send activity.
- Conditional leases, canonical queue identity and recorded-state revalidation recover missed enqueue attempts without duplicate queue records. Stale intent is stopped, retryable failures back off and stop after five. Tenant/job/event composite FKs and privacy-safe latest-100 intent status reads protect boundaries.
- Backend build/lint, 343 tests (3 existing skips), architecture/Prisma passed; unchanged UI lint/23 tests/build and fixture browser regression passed. An isolated local database applied all 14 migrations and proved transaction rollback, concurrency, acknowledgment-loss recovery and tenant FK rejection; it was removed. No provider calls.
- Migration `20260908120000_add_sms_enqueue_intents` is local-tested only and must precede a separately approved deployment. Appointment durability, recovery UI/policy for exhausted intents, remaining triggers/templates/preferences/email and live acceptance remain open. No broad acceptance box is checked.
- Planning estimate: APP-013 roughly 55%; APP-006 through APP-016 roughly 75%. Review steps and limitations are in backend APP-013 evidence.

## Bounded Read-Only Enqueue Intent Visibility (2026-09-08)

- Implementation: backend `09bdbbd83547a3ef86136a6a498b090961b9ff60`, `feat(app): show APP-013 enqueue intent status`. Owner reviewed the durable technician checkpoint and approved this continuation.
- `/app/notifications` now reads the intent endpoint independently of message history. It shows pending, acknowledged queue (not delivery), stale and exhausted states with safe failure labels, timing and IDs; client-side job/status filters explicitly apply to latest 100 tenant intents only. No send/reset/retry control exists.
- Credential/job edits and session clear invalidate both result sets and late responses. Failed intent reads do not hide successful history, or vice versa. Pending does not guarantee active retry or a send time; empty results do not prove full notification coverage.
- Passed backend build/lint, 343 tests (3 existing skips), architecture/Prisma; UI lint, 27 tests/build; local fixture desktop/390px browser QA with 18 GET-only requests. Screenshots, limitations and reproducible review steps are in backend APP-013 evidence. Existing dependency findings remain 0 critical, 4 high/9 moderate.
- No database/provider/configuration/release action. Appointment durability, recovery actions/policy, remaining triggers/templates/preferences/email/technician-recipient notifications/live acceptance remain open. Prior migration still requires separately approved deployment sequencing; no acceptance box is checked.
- Planning estimates: APP-013 roughly 58%; APP-006 through APP-016 roughly 75%.

## Bounded Initial Confirmation Intent Durability (2026-09-08)

- Owner reviewed intent visibility and approved continuation. Backend `1ea45446995bad740fbede9be51f49230f51fb6f`, `feat(app): persist APP-013 initial confirmation intents`, on PR #21.
- Initial booking finalization now atomically stores the acknowledged calendar reference, fixed confirmation SMS intent and customer audit. Matching tenant/state/window guards prevent stale finalization; after commit, queue/operations notification/logging failure cannot undo booking. The worker now recovers initial confirmations as well as technician departures with the same disabled gate, state hash and canonical queue identity.
- Failed/ambiguous database finalization after acknowledged calendar insertion retains the reservation for office review. An unfinalized same-window replay cannot falsely succeed or insert again; finalized replay does not duplicate intent. Closed/deleted jobs cannot reopen through initial confirmation. Calendar/database atomicity, pre-finalization crash repair and unknown provider outcomes remain unsolved; no automated calendar reconciliation is added.
- Passed backend build/lint, 362 tests (3 existing skips), architecture/Prisma; UI lint, 27 tests/build and synthetic Chrome desktop/390px QA (18 GET-only requests). All 14 existing migrations and real scheduling/finalization/intent transaction proof passed in a disposable local database with Calendar/notification/delivery doubles; database removed and absence verified. Provider calls zero.
- Notification-center copy and screenshots now cover initial-confirmation/technician intents. No new migration, provider/configuration, staging/production, merge, deploy, billing or real-data action. Prior intent migration remains a separate release prerequisite. Existing advisories: 0 critical, 4 high/9 moderate; existing Browserslist and pg deprecation warnings.
- Remaining: reschedule/cancellation durability, calendar reconciliation, recovery actions/policy, remaining events/templates/preferences/technician notifications/email/live acceptance. Planning estimates: APP-013 roughly 62%; APP-006 through APP-016 roughly 76%. No acceptance box is checked. Exact review commands and limitations are in backend APP-013 evidence.

## Bounded Owner/Admin Exhausted-Intent Retry API (2026-09-08)

- Backend `aab434b3ddcd83f6ccbf91ee6c19fb8a3f1adb3b`, `feat(app): add reviewed APP-013 enqueue intent retry`, on PR #21. Owner reviewed initial confirmation durability and approved continuation.
- `POST /communications/sms/enqueue-intents/:intentId/retry` is owner/admin only and API-only. Requires explicit acknowledgment, fixed reason code and exact listed `updatedAt`; only FAILED at five failures, unacknowledged, supported and unchanged job snapshots qualify. Conditional reset and privacy-safe user audit commit together. Stale/repeated/concurrent submissions cannot force another reset; dispatcher remains read-only.
- Returns pending, not queued/delivered. No direct queue/provider call or consent/delivery-enable override; worker policies and existing canonical identity remain authoritative. Each new cycle requires fresh owner review and retains the five-failure bound. No recovery button, arbitrary content, stale override or historical backfill exists.
- Validation: 403 backend tests (3 existing skips), build/lint/architecture/Prisma; UI lint/27 tests/build and 18-GET fixture desktop/390px browser regression. Real HTTP auth/role/validation/throttle tests use synthetic identity verification. Disposable-local Prisma proof confirms concurrent single reset/audit, disabled processing, stale rejection and audit rollback; all 14 existing migrations pass, fixture removed and absence verified. Provider calls zero.
- No new migration, release/provider/configuration or real-data action. Existing dependency findings remain 0 critical, 4 high/9 moderate. Prior migration and live retry use remain separately approval-gated; broad acceptance boxes stay unchecked.
- Planning estimates: APP-013 roughly 65%; APP-006 through APP-016 roughly 77%. Recovery UI/acceptance, reschedule/cancellation durability, calendar reconciliation and remaining events/templates/preferences/technician notifications/email/live acceptance stay open. Exact review steps: backend APP-013 evidence.

## Bounded Guarded Exhausted-Intent Retry UI (2026-09-08)

- Backend `9a3d86a103df675fd0f329b15922dda2ffe8736b`, `feat(app): add guarded APP-013 intent retry review UI`, pushed and remote-verified on PR #21's focused feature branch.
- Owner reviewed the preceding retry API and approved continuation on the existing focused branch. Added server-verified capability, explicit reason/acknowledgment review, snapshot-bound POST, one-in-flight protection and mandatory refresh after every outcome. Unknown/network/server/15-second timeout warns of possible prior acceptance, with no automatic retry. Token/job/session changes suppress late responses; dispatcher remains read-only. Backend current-state/tenant/audit/concurrency controls remain authoritative.
- Validation: backend lint/build, 411 tests (3 existing skips), architecture/Prisma; UI lint, 29 tests/build; synthetic Chrome desktop/390px QA, 92 mocked requests (78 GET/14 POST) and four inspected screenshots. Disposable-local real-Prisma recovery proof passed 14 existing migrations and invariant checks, zero provider calls; fixture removed and absence verified. Governance consistency/format/diff checks passed.
- Critical audit gates pass with 0 critical but backend has 5 high/10 moderate and UI 10 high/1 moderate findings. Lockfiles/dependencies unchanged; release triage/risk acceptance remains open. No provider/configuration, schema/migration, merge/deploy or real-data action. Prior migration and live retry/acceptance remain separately approval-gated; acceptance boxes stay unchecked.
- Planning estimates: APP-013 roughly 68%; governed APP-006 through APP-016 roughly 78%. Owner/live acceptance, reschedule/cancellation durability, calendar reconciliation and remaining events/templates/preferences/technician notifications/email remain open. Exact commands and evidence: backend `evidence/APP-013/readiness-report.md`. Stop for owner review.

## Bounded Post-Calendar Cancellation Intent Finalization (2026-09-08)

- Backend `df97aa83b6615b94e462f91deae3492a064aa63a`, `feat(app): finalize APP-013 cancellation notification intents`, pushed and remote-verified on PR #21's current feature branch.
- Owner reviewed guarded recovery UI and approved this single section. Version-bound cancellation claim precedes Calendar deletion; only after acknowledgment does atomic local finalization advance the job version, capture canonical cancellation intent and record privacy-safe CUSTOMER audit. Finalization failure does not reopen a deleted appointment; post-commit worker/operations/logging failure cannot undo it. Finalized replay creates nothing; missing proof requires office review.
- Cancellation is now supported by existing disabled intent processing and owner/admin reviewed retry with unchanged policy/state/deduplication gates. UI copy/fixtures cover cancellation; reschedule capture and backfill remain absent. Calendar/database cross-system atomicity, unknown deletion outcomes and pre-finalization crashes still require manual reconciliation; no end-to-end durability or independent Calendar status claim.
- Passed 425 backend tests (3 existing skips), build/lint/architecture/Prisma; UI lint/29 tests/build; 92-request synthetic desktop/390px browser QA and four inspected screenshots; disposable-local real-Prisma proof with 14 existing migrations, atomic rollback, concurrency, replay, recovery and tenant/version/compensation guards. Zero provider calls; fixture dropped and absence verified. Exact commands and boundaries: backend `evidence/APP-013/readiness-report.md`.
- No new schema/migration/dependency/provider/configuration, merge/deploy or real-data action. Audit remains 0 critical, backend 5 high/10 moderate, UI 10 high/1 moderate; release triage required. Prior migration/live acceptance remain separately gated. Planning estimates APP-013 71%; governed APP-006 through APP-016 79%. No acceptance box checked; stop for review.

## Bounded Post-Calendar Reschedule Intent Finalization (2026-09-08)

- Backend `f7d5e1ecde3900768058807e6da2dce80b40e6c8`, `feat(app): finalize APP-013 reschedule notification intents`, pushed and remote-verified on PR #21's current feature branch.
- Owner reviewed cancellation finalization and approved this section. New local rescheduling service captures canonical intent and customer activity audit atomically after Calendar acknowledgment, using original/claimed version and window guards. Existing labels remain with additive intent ID/finalized timestamp. Same-window replay needs audit proof for the current version; pending/legacy/changed records require office review. Finalization failure after acknowledgment cannot roll Calendar/local window back; post-commit notification/logging failure cannot undo success.
- All four current template types now use disabled intent processing and guarded owner/admin retry with unchanged identity/state/consent/quiet-hour gates. No backfill/reconciliation or global external-operation serialization; unknown Calendar outcomes and pre-finalization crashes remain manual. Unrelated job edits can conservatively invalidate replay proof. No end-to-end scheduling durability claim.
- Passed 443 backend tests (3 existing skips), lint/build/architecture/Prisma; UI lint/29 tests/build; 92-request synthetic desktop/390px QA and four inspected screenshots; disposable local real-Prisma proof with 14 existing migrations, atomic rollback, concurrency, replay, tenant/version, compensation and worker/retry checks. Zero provider calls; fixture removed and absence verified. Exact review steps: backend APP-013 evidence.
- No new schema/migration/dependencies/provider/configuration, live/real-data, merge or deploy action. Audit remains 0 critical, backend 5 high/10 moderate, UI 10 high/1 moderate; dependency release triage required. Planning estimates APP-013 74%; governed APP-006 through APP-016 80%. Acceptance boxes remain unchecked; stop review-ready.

## Bounded Calendar Operation Journal Foundation (2026-09-08)

- Backend `cbdddc9f3e0b661f72166b873c2f92b9d09af00d`, `feat(app): add APP-013 calendar journal foundation`, pushed and remote-verified on the existing transactional-messaging feature branch.
- Owner approved the reliability/security recommendation. On PR #21's existing focused branch, added atomic version-bound CREATE/RESCHEDULE/CANCEL local reservation plus PENDING journal, retained original target/window/label, stable create ID and desired schedule. Composite tenant/job FK, partial unique unfinished-job index and SQL action/window/version/terminal checks guard persistence. Same-window reschedule rejects a new operation. Hard deletion is restricted pending archival policy.
- Service remains unregistered and has no production caller. No API, Calendar request, recovery worker, finalization transition, success audit, SMS intent or backfill is activated. Existing runtime reconciliation/crash gaps remain; this checkpoint proves only the local foundation. Do not deploy/activate it alone or treat pending local state as Calendar success.
- Passed backend lint/build, 44 suites/464 tests (3 existing skips), architecture/Prisma; UI lint/29 tests/static build, 92-request desktop/390px synthetic QA and four inspected screenshots. Disposable local proof applied all 15 migrations and passed six actual before/after-commit process exits across three actions, restart persistence, real write rollback, concurrency, tenant/version/SQL/deletion guards and no premature messages; fixture removed, provider calls zero.
- One initial concurrent full-test process exited 139; two separate subsequent full runs passed, cause unestablished. Existing pg warning and unresolved audits: backend 5 high/10 moderate, UI 10 high/1 moderate, 0 critical. Dependencies unchanged; release gate not accepted.
- Migration `20260908180000_add_calendar_operation_journal` is local-tested only. No provider/configuration, real data, staging/production, merge/deploy, billing/secrets/IAM action. Planning estimates APP-013 approximately 75%; governed APP-006 through APP-016 approximately 80%. No acceptance box checked. Next after review: one bounded journal-aware scheduling/reconciliation section, then separately reviewed dependency remediation. Exact steps and limitations are in backend APP-013 evidence.

## Commit Convention

- Commit message: `feat(app): implement APP-013 notification center templates`
- One focused commit only.
