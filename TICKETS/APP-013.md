# APP-013 - Notification Center and Messaging Templates

## Ticket Summary

- Ticket ID: `APP-013`
- Repo: `signmons-calldesk-backend` (+ frontend app surface)
- Screen IDs: `SCR-APP-021`, `SCR-APP-022`, `SCR-TECH-005`
- Objective: centralize owner/dispatcher/technician/customer communication signals and template controls.

## In Scope

- Notification center for owner/dispatcher and technician events.
- Messaging template management for customer communications.
- Transactional SMS and email templates for customer, technician, dispatcher, and owner communications, selected according to tenant policy, consent, event type, and delivery availability.
- Event categories:
  - intake created
  - urgency escalation
  - assignment update
  - payment status update
  - schedule confirmation/update
- Transactional appointment email after the calendar booking is committed:
  - ask for and retain the customer email once without repeating captured questions
  - confirmed date and Eastern Time arrival window
  - appointment reference and concise service type
  - private, absolute customer-management link
  - add-to-calendar attachment or standards-compliant calendar action
  - call/text fallback and private-link warning
- One updated customer email after a successful reschedule and one cancellation email after a successful cancellation.
- Delivery failure must not roll back a confirmed appointment; it creates an operator-visible failure with a safe retry path.
- Customer management credentials must not be copied into analytics, normal application logs or message-preview telemetry.
- Delivery status visibility (sent/failed/retry).
- SMS consent, STOP/HELP, quiet-hour, duplicate-prevention, provider-status, and safe-retry behavior must use the BE-008 communications foundation.

## Out of Scope

- External marketing campaign automation suite.
- Multi-channel social messaging orchestration.
- Advanced A/B experimentation framework.

## Acceptance Criteria (must all pass)

- [ ] Notification center displays role-appropriate operational events.
- [ ] Templates can be managed per tenant policy.
- [ ] Transactional SMS and email are independently configurable by permitted event, recipient role, consent state, and tenant brand policy.
- [ ] Appointment confirmation, reschedule, cancellation, assignment, technician-status, payment-status, and dispatcher-alert SMS events are idempotent and retain provider delivery/failure status.
- [ ] Delivery status and failures are visible.
- [ ] A committed eligible appointment sends exactly one branded customer confirmation email with the confirmed window, reference, private management action and calendar action.
- [ ] Reschedule sends exactly one updated confirmation; cancellation sends exactly one cancellation notice without an active management action.
- [ ] Customer email is requested once, retained in the conversation/job record and never repeatedly requested after capture.
- [ ] Customer email-delivery failure does not undo the job or calendar event and is visible to operations for retry.
- [ ] Secure management credentials are absent from analytics, ordinary logs and operator message previews.
- [ ] Sensitive data is redacted per role boundaries.
- [ ] Notification events are auditable.

## Test and Evidence Plan

- Current bounded UI section: read-only `/app/notifications` (`SCR-APP-021`) using the existing tenant-bound SMS history endpoint. Include status/job filters, empty/error/loading states and responsive QA. No send/replay/template-edit controls; durable enqueue recovery remains open.

- Backend gates: `npm run -s build`, `npm test -- --runInBand`, `npm run -s arch:check`
- Frontend gates (if touched): `npm run -s build`, `npm run -s lint`, `npm test -- --runInBand`
- Evidence:
  - notification center screenshots
  - template configuration proof
  - failed-delivery handling proof

## Risks

- Template drift from policy/compliance constraints.
- Alert fatigue for dispatch roles.
- Overexposure of customer data in message previews.
- Forwarded appointment-management links acting as bearer credentials; copy must warn customers to keep the link private and destructive actions may require an additional verification policy.

## Bounded Queue State Checkpoint (2026-09-08)

- Backend PR #21 provides four versioned customer SMS templates, tenant-bound operator queue/history and queue-admission validation against stored job/technician state.
- State-check implementation commit: `a04f1d7129495ed687a5ac896c5f29dd022a036c`. UI regression lint, 17 tests and build also pass; no new rendered-browser acceptance is claimed.
- Deleted jobs return 404; contradictory lifecycle copy or incomplete schedule data returns 409 before queue access. Valid cancellation, reschedule and EN_ROUTE cases remain eligible for the existing BE-008 controls.
- Backend validation: 16 focused and 302 full tests passed, 3 existing policy skips; build/lint/architecture/Prisma/critical audit passed. No UI changed and no provider send occurred.
- Admission uses a snapshot; concurrent mutations, completed-reschedule proof, automatic triggers and stale queue checks remain open alongside email, templates/preferences, UI and acceptance. Acceptance boxes stay unchecked.
- Estimate: APP-013 roughly 20%; governed APP-006 through APP-016 roughly 70%.

## Bounded Appointment Lifecycle Checkpoint (2026-09-08)

- Backend PR #21 now queues confirmation, reschedule and cancellation SMS only after the matching canonical appointment/calendar operation commits. Queue failure is isolated from the appointment result.
- Each automatic event uses a deterministic template/state digest as its idempotency identity. Manual queueing records the same digest, and the delivery worker revalidates it against the tenant-scoped job immediately before provider access.
- Missing/deleted, contradictory or changed state is dead-lettered before send; unverifiable earlier transactional records fail closed. The digest covers the rendered recipient, brand/timezone and schedule or technician state without exposing raw values in operator history.
- Focused validation passed 3 suites / 44 tests; full backend build/lint, 36 suites / 308 tests with 3 existing skips, architecture, Prisma and critical audit passed. Unchanged UI lint, 17 tests and build also passed; no new rendered browser artifact is claimed.
- Outbound delivery remained disabled. No provider message, configuration, migration, merge, deployment or real customer/job mutation occurred. A narrow check-to-provider concurrency race remains explicit.
- Automatic assignment/technician/payment/dispatcher events, email, preferences, UI and acceptance remain open. Estimate: APP-013 roughly 35%; governed APP-006 through APP-016 roughly 72%, planning estimates only.

## Bounded Technician On-The-Way Checkpoint (2026-09-08)

- Implementation: backend `477048abfa22a15ec63642f83519c180354341ea`, `feat(app): queue APP-013 technician departure notifications`.
- Existing technician status endpoint queues customer on-the-way SMS only after a changed status and audit commit. Duplicate/no-op retries and rejected/rolled-back writes do not queue. Queue/logging failure does not undo field status.
- Identity includes technician status timestamp; stale departure or reassignment fails the existing pre-send check. Earlier on-the-way hashes fail closed. No API response shape, UI, schema or provider configuration changed.
- Validation: 323 backend tests (3 existing skips), build/lint/architecture/Prisma, critical audit, unchanged UI lint/17 tests/build. Fifteen new regression cases cover ordering, failure boundaries, identity and obsolete delivery. No provider send occurred.
- Durable enqueue recovery is not implemented: post-commit failure/crash can leave status without a message, and status replay is not auto-repair. Remaining triggers, email, preferences, UI and acceptance stay open; no acceptance box is checked.
- Planning estimate: APP-013 roughly 40%; APP-006 through APP-016 roughly 73%. Review backend PR #21 and `evidence/APP-013/readiness-report.md`.

## Bounded Read-Only Notification Center (2026-09-08)

- Implementation: backend `3330c7bfa011120fa26f10b6582ab471f2ad3728`, `feat(app): add APP-013 read-only notification center`.
- `/app/notifications` (`SCR-APP-021`) consumes only SMS history with latest-100/query limits, status/job filtering, explicit UTC, and sent-versus-delivered distinctions. Navigation is available from dispatch. No send/replay/template-edit control is included.
- Password-masked operator token stays in memory; token/job changes and session clear invalidate prior responses. Generic errors and explicit metadata rendering exclude raw sensitive fields.
- Passed 323 backend tests (3 existing skips), backend build/lint/architecture/Prisma; UI lint, 23 tests/build; synthetic Chrome desktop/390px browser QA for filters, loading/empty/403/500, late-response/session clearing, no token storage and private-field omission. Screenshots and exact commands are in backend APP-013 evidence.
- Live backend/operator acceptance, durable enqueue recovery, remaining triggers, template/preferences controls, technician notification UI and email remain open. Broad acceptance boxes remain unchecked. No provider send or release occurred.
- Planning estimate: APP-013 roughly 50%; APP-006 through APP-016 roughly 74%.

## Commit Convention

- Commit message: `feat(app): implement APP-013 notification center templates`
- One focused commit only.
